# RabbitMQ Topology Visualizer — Task Backlog

Automation rule: each 2-hour run should complete at least one unchecked task when possible, run verification, request OpenAI review, then commit and push approved changes to GitHub.

## Bootstrap

- [x] Create Vite + React + TypeScript app scaffold. (package.json, vite.config.ts, tsconfig*.json, index.html, src/main.tsx, src/App.tsx)
- [x] Add Vitest and baseline `test`, `typecheck`, and `build` scripts. (vitest devDep, `test`/`test:watch` scripts, `test` block in vite.config.ts, test/smoke.test.ts)
- [x] Add project-local `AGENTS.md` with coding/automation rules. (AGENTS.md — landed in bootstrap; checkbox was stale)
- [x] Add sanitized minimal RabbitMQ definitions fixture. (test/fixtures/minimal-definitions.json)

## Core model and parsing

- [x] Define canonical topology TypeScript model and stable ID helpers. (src/core/model/topology.ts, src/core/model/ids.ts, src/core/model/index.ts + 13 unit tests in test/core/model/ids.test.ts)
- [x] Implement JSON shape classifier for definitions exports vs split management dumps. (src/core/parse/jsonClassifier.ts with filename + content heuristics, host/vhost path hints, adds `management-dump-vhosts`; 15 unit tests in test/core/parse/jsonClassifier.test.ts)
- [x] Implement RabbitMQ definitions export parser for exchanges, queues, bindings, vhosts, policies, and parameters. (src/core/parse/definitionsParser.ts with `parseDefinitionsExport`, ID resolution, inferred-vhost + duplicate + missing-source-destination diagnostics, dead-letter arg extraction, cluster hint from `global_parameters`; keeps sanitized `parameters` as `RawParameter[]` for the shovel/federation task, including array-valued federation upstream sets; 17 unit tests in test/core/parse/definitionsParser.test.ts)
- [x] Implement split management dump parser for `queues.json`, `exchanges.json`, `bindings.json`, `parameters.json`, and `policies.json`. (src/core/parse/splitDumpParser.ts with `parseSplitManagementDump` composing over `parseDefinitionsExport`, per-file sourceFileId tracking on host, `split-dump.file-not-array` diagnostic; 6 unit tests in test/core/parse/splitDumpParser.test.ts)
- [x] Implement runtime parameter parsing for shovels and federation upstreams. (src/core/parse/runtimeParameters.ts with `parseRuntimeParameters` producing `Shovel[]` / `FederationLink[]` / `FederationUpstreamSetEntry[]`; consumes AMQP URI parser for redaction + host/vhost hints; supports dashed + underscored keys; 11 unit tests in test/core/parse/runtimeParameters.test.ts)
- [x] Implement AMQP URI parser that redacts credentials and extracts host/vhost hints. (src/core/parse/amqpUri.ts with `parseAmqpUri` and `redactAmqpUri`, no raw/user credential fields in parsed output, query/fragment-safe URL parsing, IPv6 + percent-encoded vhost support, default port from scheme; 16 unit tests in test/core/parse/amqpUri.test.ts)
- [x] Implement RAR archive import support for the current `Downloads.rar` sample. (adds `node-unrar-js` dep, src/core/parse/rarLoader.ts with `loadRarArchive` + `filterJsonEntries`, path-string filter, unsafe path + encrypted-entry diagnostics, compressed archive / entry count / per-entry bytes / total bytes limits; MIT-licensed FolderTest.rar fixture in test/fixtures/rar/; 10 unit tests in test/core/parse/rarLoader.test.ts covering round-trip + error paths + safety limits)
- [x] Implement diagnostics for malformed JSON, missing references, duplicates, and unresolved endpoints. (adds `safeParseJson` in src/core/parse/safeJson.ts with `parse.{malformed-json,empty-input,non-string-input}`; adds src/core/resolve/{diagnostics.ts,index.ts} with `dedupeDiagnostics`, `groupBySeverity`, `sortBySeverity`, `summarizeDiagnostics`; existing parsers already emit the missing-reference/duplicate/unresolved-endpoint codes surfaced by these helpers; 10 unit tests across test/core/parse/safeJson.test.ts + test/core/resolve/diagnostics.test.ts)

## Graph and query engine

- [x] Build graph nodes/edges from canonical topology. (adds src/core/model/graph.ts with `GraphNode`/`GraphEdge`/`GraphNodeKind`/`GraphEdgeKind`; adds src/core/graph/buildGraph.ts + index.ts with `buildGraph` producing host/vhost/exchange/queue/shovel/federation/external nodes and contains/binds/alternate-exchange/dead-letter/shovels/federates edges, `contains` edges also link vhost→shovel and vhost→federation; external node ids AND labels sanitize each source segment via `safeSegment` *before* percent-encoding so any URI embedded in `ref.host`/`ref.vhost`/`ref.exchange`/`ref.queue` can't hide behind `%3A%2F%2F`; a final defensive gate runs every returned node, edge, and diagnostic through `deepSanitize` so any `amqp[s]://…` substring (nested, embedded, whitespace-prefixed, in labels, routing keys, arguments, or diagnostic messages) is redacted via `redactAmqpUri`; emits `graph.{alternate,dead-letter}-exchange-unresolved` diagnostics; 25 unit tests in test/core/graph/buildGraph.test.ts including per-kind leak checks (exchange args, queue args, binding routing key, diagnostic message, every external-ID source field) and a whole-graph credential-leak check that poisons every node kind + every edge kind + a diagnostic)
- [x] Build indexes by exact name, kind, host, and vhost. (adds src/core/graph/indexes.ts exporting `IndexedEntity`, `IndexedEntityKind`, `TopologyIndexes`, `BuildIndexesInput`, `buildTopologyIndexes`; extends src/core/graph/index.ts barrel; index accessors return defensive copies so callers can mutate without corrupting internal state; 8 unit tests in test/core/graph/indexes.test.ts including end-to-end from the sanitized fixture)
- [x] Implement exact and ambiguous search for queues/exchanges. (adds src/core/query/findEntity.ts + index.ts exporting `findEntity`, `EntitySearchKind`, `EntitySearchOptions`, `EntitySearchResult`, `EntitySearchGroup`; exact-name lookup over `TopologyIndexes` with optional kind/host/vhost filters, `ambiguous` flag when >1 match survives, results grouped by (host, vhost) for UI-friendly disambiguation; 9 unit tests in test/core/query/findEntity.test.ts)
- [x] Implement fuzzy search for queue/exchange names. (adds src/core/query/fuzzyFindEntity.ts + barrel export exporting `fuzzyFindEntity`, `FuzzySearchOptions`, `FuzzySearchMatch`, `FuzzyMatchReason`; case-insensitive scorer with strictly disjoint tier bands — exact=1.0, prefix∈[0.80,0.85], substring∈[0.55,0.70], subsequence∈[0.30,0.50] — so `exact > prefix > substring > subsequence` is a hard invariant; stable sort by score desc then name asc; host/vhost/kind filters plus clamped `limit` (negative/0 → [], non-finite → default) and `minScore` (negative → 0, non-finite → default, >1 legitimately filters everything); 17 unit tests in test/core/query/fuzzyFindEntity.test.ts)
- [x] Implement topic routing-key matcher. (adds src/core/graph/topicMatcher.ts exporting `matchTopicRoutingKey`, `isTopicPattern`; word-tokenized memoized DP over `(patternIndex, keyIndex)` — O(P·K) time and space, so adversarial `#.#.#.…` patterns can't backtrack combinatorially; handles `*` (exactly one word), `#` (zero or more words) at any position, multiple `#`s, and empty words from leading/trailing/consecutive dots; extends src/core/graph/index.ts barrel; 24 unit tests in test/core/graph/topicMatcher.test.ts including adversarial-'#' timing bounds and empty-word coverage)
- [x] Implement upstream traversal for queue targets. (adds src/core/graph/traversal.ts exporting `upstreamForQueue`, `traverseUpstream`, `UpstreamTraversalOptions`, `UpstreamTraversalResult`, `UpstreamPath`, `UpstreamStep`; BFS with global visited so each ancestor node is expanded at most once → O(V+E) time regardless of density/cycles; reverse-walks `binds`/`routes`/`alternate-exchange`/`shovels`/`federates` (and optionally `dead-letter`) from a queue target, returns shortest-path per source + sorted reachable ancestor set + cycle/diamond marker set + maxDepth truncation flag; `maxDepth` is normalized (NaN/±Infinity → default 32, negative → 0, fractional → floor); extends src/core/graph/index.ts barrel; 15 unit tests in test/core/graph/traversal.test.ts including e2e chain, shovel→external, cycle guard, diamond graph, alternate-exchange, federation, dead-letter opt-in on/off, and invalid maxDepth values)
- [x] Implement upstream traversal for exchange targets. (adds `upstreamForExchange` to src/core/graph/traversal.ts — same shared BFS engine as `upstreamForQueue`, target-kind guard now accepts `exchange` nodes; 6 unit tests in test/core/graph/upstreamForExchange.test.ts covering exchange-to-exchange chain, target-kind rejection for queue/unknown/host, and shovel+federation into an exchange target)
- [x] Implement cross-host shovel/federation traversal. (adds src/core/graph/crossHost.ts exporting `summarizeCrossHostAncestry`, `CrossHostSummary` — extracts distinct in-project ancestor host ids, external hostname hints from `external:` nodes, and a `crossedShovelOrFederation` flag; filters ancestor host ids to only hosts that are loaded as `kind:"host"` nodes so a stale hostId on a shovel/federation never masquerades as an in-project ancestor; existing BFS traversal already walks shovel/federation edges → tests verify end-to-end reverse-walk from a queue on rabbit-b back through a shovel/federation to an exchange on rabbit-a; extends src/core/graph/index.ts barrel; 5 unit tests in test/core/graph/crossHost.test.ts covering shovel-crossing, federation-crossing, single-host baseline, and external-hostname-hint fallback when the remote host isn't loaded)
- [x] Implement cycle detection and max-depth traversal limits. (adds src/core/graph/cycles.ts exporting `findCycles`, `Cycle`, `CycleWitness`, `FindCyclesOptions` — iterative Tarjan's SCC in O(V+E) identifies every strongly-connected component that contains a cycle; each result carries the full SCC in `nodeIds` PLUS a `witness` produced by a **BFS with globally-visited + parent pointers** that returns the shortest simple cycle back to the deterministic start node in O(|SCC| + edges-within-SCC) — no combinatorial blow-up on dense/branching SCCs; correctly handles non-Hamiltonian SCCs like `A↔B, A↔C` (SCC has three nodes, witness is a valid two-node sub-cycle); singleton SCCs are reported only with a self-loop; edge-kind filter defaults to routing edges (excludes `contains`/`dead-letter`); max-depth handling was already in `traverseUpstream` via `normalizeMaxDepth` — added a 50-hop chain regression test asserting default `maxDepth=32` truncates and `maxDepth=100` reaches everything; extends src/core/graph/index.ts barrel; 10 unit tests in test/core/graph/cycles.test.ts covering DAG, 2-node cycle, 3-node cycle, non-Hamiltonian SCC + witness validity, multi-cycle SCC, self-loop, cross-host cycle spanning shovel + federation, edge-kind restriction, dense-SCC K_30 witness under 100 ms, and the max-depth regression pin)
- [x] Implement path explanation output. (adds src/core/query/pathExplain.ts exporting `explainUpstreamPath`, `PathExplanation`, `ExplainedStep`; renders each `UpstreamPath` step as a human-readable one-line sentence in source→target flow direction — verb-first per edge kind: `binds`/`routes` say "binds via routing key 'K' to …" (or "with no routing key" when empty), `alternate-exchange` says "forwards unroutable messages to its alternate: …" (noun appears once — the `to` renders as `exchange 'X'` already), `dead-letter` says "dead-letters expired or rejected messages to …" with routing key when present, `shovels`/`federates` name the shovel/federation link when it has a label and fall back to "shovel/federation link carries/mirrors messages from…" when unlabeled; node references carry host/vhost context extracted from `GraphNode.data`; all interpolated node labels, host/vhost labels, routing keys, and shovel/federation labels run through `sanitizeInline` that replaces C0 control chars + DEL with a visible middle-dot marker, so the "one line per step" and newline-joined-summary contracts hold even when a RabbitMQ name contains a stray newline/tab/CR; extends src/core/query/index.ts barrel; 9 unit tests in test/core/query/pathExplain.test.ts covering binding chains, shovel + alternate-exchange, dead-letter with routing key, empty routing-key formatting, real federation paths (labeled + unlabeled), and control-character sanitization in exchange names / routing keys / shovel labels)

## Current priorities (added 2026-08-29)

- [x] Make the entire page responsive and able to stretch with the available screen size. (index.html now ships a global reset: `html, body, #root { margin: 0; min-height: 100vh; box-sizing: border-box }` and `*, *::before, *::after { box-sizing: inherit }` so downstream inline styles inherit the safe box model. `overflow-x: hidden` is intentionally NOT set (reviewer regression: hiding horizontal overflow silently clips controls that leaked past the viewport, masking real layout bugs); overflow is prevented structurally by the fluid panel/graph rules instead. src/App.tsx replaces the fixed `padding: "2rem"` with `appMainStyle` — `padding: clamp(0.75rem, 2vw, 2rem)`, `minHeight: 100vh`, `width: 100%`, `maxWidth: 100%`, `boxSizing: border-box` — so ultra-narrow phones get a 0.75rem gutter, wide desktops keep the 2rem inset, and the shell fills the viewport without pushing past 100vw. The `<h1>` now scales via `clamp(1.3rem, 3vw, 2rem)` so it stays proportional at every breakpoint. Every panel wrapper (ImportPanel, EntitySearchBox, TopologySummaryPanel, TopologyGraphCanvas) drops its fixed pixel `maxWidth` (was 640 / 720 / 720 / 1080) and switches to `width: 100%`, `maxWidth: 100%`, `boxSizing: border-box`, `padding: clamp(0.75rem, 2vw, 1rem)` — panels stretch to fill the App shell at every viewport size instead of clipping on narrow screens or leaving dead space on wide ones. TopologyGraphCanvas's graph-container div now uses `graphContainerStyle(heightPx)` returning `height: min(70vh, ${heightPx}px)`, `width: 100%`, `boxSizing: border-box` — no hard pixel floor (reviewer regression: `clamp(320px, 70vh, …)` pinned the graph to 320px on short mobile viewports, exceeding the visible page once headers/panels above the graph consumed vertical space; the `min()` form drops the floor and lets the graph occupy 70 % of viewport height, capped at the caller's ideal ceiling). The three flex-based control rows in TopologyGraphCanvas (headerRow, selectionBar, focusBar) now declare `flexWrap: "wrap"` + `gap: "0.5rem"` so controls reflow onto multiple lines at narrow widths instead of clipping. TopologyGraphCanvas also attaches a `ResizeObserver` to the graph container (`graphContainerRef` + a new `useEffect`) that calls `rfInstance.fitView({ duration: 0 })` on every observed size change — React Flow does NOT auto-refit on container-only size changes (reviewer regression: the earlier claim that ReactFlow re-fits on resize was unsupported); `fitView` is idempotent, so the extra call at mount is invisible to the user, and the ref/observer path is guarded by `typeof ResizeObserver === "undefined"` for non-DOM test environments. 7 layout regressions in test/ui/responsiveLayout.test.tsx (rewritten to satisfy the reviewer's "inline-style-string checks aren't enough" bar): (a) index.html no longer contains `overflow-x: hidden` (regex over the raw file, after stripping CSS block comments so the "why removed" note doesn't false-positive), while the border-box + min-height reset survives; (b) mechanical fixed-pixel scan — after mounting ImportPanel, EntitySearchBox, and TopologySummaryPanel, walk every rendered descendant and fail if any of them pins `width`/`min-width`/`max-width` to a fixed pixel value > 320 px (narrow-phone width) — this is the mechanical guard that catches any future reintroduction of `maxWidth: 640` on a panel wrapper, the exact bug this task fixed; (c) App shell — same fixed-pixel scan across the full mounted app tree, plus semantic assertions that `<main>` is `width:100%` / `minHeight:100vh` / `padding: clamp(...)`; (d) TopologyGraphCanvas control rows regression — mount with focus+selection active so all three flex rows render, and assert every one declares `flexWrap:"wrap"` (a wiring bug that dropped `flexWrap` on any row would clip controls on narrow viewports); (e) graph shell height regression — inline `height` matches `^min(` AND contains `vh` AND contains the ideal `${heightPx}px`, and does NOT match `clamp(3\d\dpx` (the removed hard-320px-floor form); (f) ResizeObserver → fitView regression — install a `MockResizeObserver` (jsdom ships no ResizeObserver) plus a ReactFlow stub that records the `fitView` spy handed to `onInit`, mount the canvas, wait until the observer records the graph container, snapshot the baseline `fitView` call count from mount-time effects, then trigger three observer notifications and assert each one increments the spy — the exact wiring the reviewer flagged as unsupported now has a callable-count regression; (g) simulated narrow-viewport (320 px `innerWidth`) — re-run the mechanical fixed-pixel scan across every panel to prove the guard holds AT a real narrow viewport size.
- [x] Add a full-page graph mode. Provide an accessible toggle that lets `TopologyGraphCanvas` occupy the whole application viewport, preserve the loaded topology, selection, focus, filters, and visibility state while entering/leaving the mode, refit the graph after the size change, and support a clear exit action (including Escape where appropriate). Add UI regressions for enter, resize/refit, state preservation, and exit. (TopologyGraphCanvas gained an `isFullPage` `useState` + memoized `toggleFullPage`/`exitFullPage`; a new `topology-graph-fullpage-toggle` button in the header row carries `aria-pressed`, an accessible `aria-label`, and toggles between "Full page" / "Exit full page (Esc)" labels. When active the `<section>` swaps to a `fullPagePanelStyle` with `position: fixed`, `inset: 0`, `zIndex: 1000`, column flex, `background: #fff`, `overflow: auto` — so the overlay covers the App shell without leaking clicks through, and auxiliary panels remain reachable on short viewports. The graph shell swaps to `fullPageGraphShellStyle` — `flex: 1 1 auto`, `minHeight: 50vh` — which drops the `min(70vh, heightPx)` cap so the graph fills remaining vertical space while keeping React Flow's own controls reachable on tiny screens. `data-fullpage` mirrors the flag for regression assertions. Refit wiring: the existing `useEffect` keyed on `[focused]` now also depends on `isFullPage`, and the existing ResizeObserver on the graph container fires on the size delta produced by entering/leaving full-page — both paths call `fitView({duration:0})`, which is idempotent so double-firing is invisible. Exit action: an `useEffect` gated on `isFullPage` binds `keydown` at document level and calls `exitFullPage()` on `Escape`, and locks `document.body.style.overflow = "hidden"` while active (restoring the previous value on cleanup) so the user does not get a dual-scrollbar UX. Both the document listener and body-overflow mutation are guarded against non-DOM test environments via `document?.body`. State preservation is structural: every piece of user state — selection (`internalSelectedNodeId`), focus (`focusNodeId` from props or controlled), filters, visibility, `showContains`, configured-flow pause — lives in stable hooks on `TopologyGraphCanvas` itself, so toggling `isFullPage` is a pure re-render and never unmounts the canvas subtree. Tests in `TopologyGraphCanvas.test.tsx > "TopologyGraphCanvas — full-page mode"` cover: (a) enter regression — `aria-pressed` flips false→true, `data-fullpage` flips false→true, `position:fixed` + `inset:0`/`0px` + `zIndex >= 100` all applied; (b) graph shell grows into remaining space via `flex:1` with a `vh|px` minHeight, no `min(...)` cap; (c) Escape at document level exits and clears `position:fixed`; (d) clicking the toggle a second time exits; (e) body scroll is locked to `hidden` on enter and restored to the pre-enter value on exit; (f) state preservation — mount with focused mode active (via prop), prime EVERY internal state (selection, `showContains`, filters via the routing-key input, visibility via "hide selected", configured-flow pause via the pause button), then toggle enter and exit, asserting each of them survives BOTH transitions — plus a hard structural check that `onInitCalls` on the ReactFlow mock stays flat across the toggle (proves the subtree did not remount, which is the structural precondition for state persistence); (g) refit wiring: the `fitView` counter increments after every enter and every exit while `onInitCalls` stays flat — proving the increment came from the `[focused, isFullPage]` `useEffect` or the ResizeObserver path, not from a spurious ReactFlow remount that would re-invoke `onInit`; (h) refit specificity — an unrelated re-render (flipping the contains-toggle) does NOT tick fitView, then two subsequent full-page toggles tick fitView by EXACTLY one each, matching the `[focused, isFullPage]` effect wiring. The reviewer's earlier concerns — "state preservation only covers selection + showContains" and "refit test can pass because ReactFlow mock re-invokes onInit on rerender" — are now both structurally disproven: the mock tracks `onInitCalls` and every state-persistence assertion covers a distinct piece of internal state.
- [ ] Complete the selected queue/exchange flow explorer across every loaded host and vhost, building on the existing name-driven focused-flow task below rather than introducing a second traversal engine. After explicit queue/exchange selection (with host/vhost disambiguation when names repeat), compute all cycle-safe upstream and downstream routes through exchange-to-exchange and exchange-to-queue bindings, alternate/dead-letter behavior where enabled, shovels, federation links, and unresolved external endpoints. Show both (a) one or more explicit source-to-destination chain views and (b) the corresponding focused graph. Each chain step must explain binding/routing restrictions and conditional semantics so the UI does not imply that every message necessarily follows the route. Preserve every applicable branch within the depth limit and clearly report cycles, truncation, ambiguity, and unresolved links. Include an end-to-end regression equivalent to `q50 -> shovel1 -> exchange1 -> exchange3 -> queue40 -> shovel2 -> exchange4 -> queue1 -> shovel3 -> exchange5 -> queue30`, proving that selecting an entity in the middle (such as `queue1`) exposes all applicable routes in both directions across vhosts.
- [ ] Add first-class hash-queue and controller concepts. Identify and document the supported input shapes/metadata that denote hash queues and their controllers, extend the canonical model and stable IDs, parse them without breaking ordinary RabbitMQ queues, represent controller-to-hash-queue relationships in traversal and graph output, and expose distinct labels/details/filters in the UI. Unknown or partially resolved controller relationships must produce actionable diagnostics instead of being silently dropped. Add sanitized fixtures plus parser, graph, traversal, and UI tests covering duplicate names across hosts/vhosts and malformed/missing controller references.

## UI

- [x] Implement real multi-JSON batch import in the UI and core. (extends `ImportResult["archiveKind"]` with a new `"batch"` variant + adds `BatchFileInput` / `BatchImportInput` types + `importTopologyBatch(input): Promise<ImportResult>` in src/core/import/importArchive.ts — reuses the existing two-phase `processJsonEntries` pipeline (definitions files parse eagerly; management-dump files stage by host then group-parse via `parseSplitManagementDump`) so a `queues.json` + `exchanges.json` + `bindings.json` triple selected together produces one coherent `ImportResult` where bindings resolve across files; enforces per-batch limits — `maxEntryCount` (rejects but still records every source filename as `kind: "load-error"` so the picker list stays visible), `maxEntryBytes` (records the oversized file as `kind: "load-error"` and continues with the rest), and cumulative `maxTotalDecompressedBytes` (marks the crossing file and every subsequent file as `kind: "load-error"` — no filename is ever silently dropped); non-JSON files are recorded as `kind: "non-json"` and skipped rather than aborting; every source filename is retained on `ImportedFile.path` and every diagnostic emitted by the batch importer carries a batch-index-disambiguated `sourceFileId` of the form `batch[N]:<fileName>` so duplicate filenames (e.g. two `queues.json` from different hosts selected together) produce distinct diagnostic ids while the display `path` stays as the plain filename (which is what host-derivation heuristics like `deriveHostFromPath` consume); both `BatchFileInput` and `BatchSkippedInput` carry an optional `selectionIndex: number` field so a UI that splits its selection into readable and skipped arrays can pin every diagnostic to the file's ORIGINAL picker position — the batch importer honours this on both the readable and skipped code paths so an interleaved [readable, skipped, readable] selection of three `queues.*` files still produces `batch[0]:queues.json`, `batch[1]:queues.zip`, `batch[2]:queues.json` diagnostics in exact picker order (never the array-position collision the reviewer flagged); adds an optional `skipped: BatchSkippedInput[]` on `BatchImportInput` for callers that preflighted a file and chose not to read it — the batch importer merges these entries into `ImportResult.files` with `kind: "load-error"` and matching `import.preflight-{entry-too-large,total-size-exceeded,unprocessed}` diagnostics; adds `import-batch` request kind to `WorkerRequest`/`WorkerResponse` in src/core/import/importArchiveWorkerMessage.ts, dispatcher case in `handleImportArchiveMessage`, `importBatch(input): Promise<ImportResult>` on `ImportArchiveWorkerClient` (both worker-backed and `createMainThreadClient` implementations); refactors src/ui/components/ImportPanel.tsx — file input has `multiple` set, picker and drop handler consume every selected file via `Array.from(FileList)` instead of `files[0]`, single-file selections still route through the existing `importArchive` path so backwards-compatible `archiveKind: "rar"|"zip"|"json"` labels + `Loaded JSON` summary text are preserved for all previous tests, multi-file selections run a **complete** preflight against `IMPORT_DEFAULT_LIMITS` synchronously (no I/O) BEFORE reading any bytes — `maxEntryCount` is checked FIRST (an over-cap selection immediately produces `preflight-too-many-files` skips for every file with zero `arrayBuffer` calls), then per-file `maxEntryBytes`, then cumulative `maxTotalDecompressedBytes`, and any archive (`.rar`/`.zip`) selected alongside JSON gets a `preflight-non-json-in-batch` skip because multi-file mode is JSON-only (single-file mode still handles archives through `importArchive`); every failing file is shipped to the batch importer via the `skipped` payload so their filenames still appear in the result but no memory is ever allocated for their bytes; the surviving files are read *sequentially* (`for/await` instead of `Promise.all`) with each `File.arrayBuffer()` call wrapped in try/catch so a per-file read failure becomes a `read-failed` skip and the loop continues — the batch is never aborted by one bad file, and every other valid file still contributes; multi-file selections route through `workerClient.importBatch({ files, skipped })`, batch loading state shows `Loading N files…` and completion shows `Loaded batch of N files`, `onImported` fires exactly once per selection; 11 new core tests in test/core/import/importBatch.test.ts (empty batch → warning + empty summary, single-file parity, three-file split-dump group parse with cross-file binding resolution + clean-path invariant, duplicate-filename disambiguation on `Diagnostic.sourceFileId` (two malformed `queues.json` files produce distinct `batch[0]:queues.json` and `batch[1]:queues.json` ids while the display path stays plain), mixed valid/malformed with per-file diagnostics, non-JSON skip, `maxEntryCount` cap that still records every source filename as load-error, per-entry byte cap that records the oversized file as load-error and continues, cumulative-size cap that records the crossing + subsequent files as load-error, and a `skipped` preflight payload path that surfaces preflight-skipped files with matching `import.preflight-*` diagnostics, and a `selectionIndex` interleave regression that feeds two malformed `queues.json` at picker positions 0 and 2 plus a skipped `queues.zip` at position 1 through the importer's readable/skipped arrays and asserts the emitted `sourceFileId`s are exactly `batch[0]:queues.json`, `batch[1]:queues.zip`, `batch[2]:queues.json` — no batch[1]:queues.json collision, no batch[2]:queues.zip mis-attribution); 1 new worker dispatcher test that round-trips an `import-batch` request through `handleImportArchiveMessage`, plus 1 client-level regression driving `createImportArchiveWorkerClient(...).importBatch(...)` end-to-end through a shim worker with an interleaved files-plus-skipped payload and asserting the posted request kind is `import-batch` AND the returned diagnostics carry both `batch[0]:queues.json` and `batch[1]:queues.json` (proving the whole client pipeline preserves `selectionIndex`); 10 new ImportPanel jsdom tests (asserts `input.multiple === true`, two selected split-dump files both contribute exchange + queue entities to `onImported`'s combined `ImportResult`, drag-and-drop parity for two-file selection, mixed valid + malformed batch reports `load-error` alongside the parsed file, a preflight-safety regression where a File whose `size` is 100 MB (past the 25 MB per-entry cap) has its `arrayBuffer` replaced with a throwing spy — the test asserts the spy is NEVER called, yet the oversized filename still appears in the combined `ImportResult` as `kind: "load-error"` with an `import.preflight-entry-too-large` diagnostic; an entry-count preflight regression that constructs a 5 001-file selection where every File's `arrayBuffer` is a tripwire that throws — asserts none of the tripwires fire, every filename appears as load-error, and the `import.preflight-too-many-files` diagnostic is emitted; a per-file read-failure regression pairing a valid `queues.json` with a `broken-read.json` whose `arrayBuffer` throws — asserts the broken file becomes a `read-failed` load-error entry, the `import.read-failed` diagnostic is emitted, AND the valid file's queue entities still appear in the combined result (the batch is not aborted by the single bad read); an archives-in-batch preflight regression pairing a `queues.json` with a `topology.zip` — asserts the zip is a `preflight-non-json-in-batch` load-error entry with the matching diagnostic and the zip's `arrayBuffer` is NEVER called, so multi-file mode is unambiguously JSON-only; a picker-order interleaved-duplicates regression selecting [queues.json@0 malformed, queues.zip@1 skipped, queues.json@2 malformed] and asserting the emitted diagnostic `sourceFileId`s are exactly `batch[0]:queues.json`, `batch[1]:queues.zip`, `batch[2]:queues.json` (no batch[1]:queues.json collision, no batch[2]:queues.zip mis-attribution); and a drag-drop parity regression for the same interleaved-duplicates case proving the picker-order attribution is preserved end-to-end through the drop path too); all previous ImportPanel tests still pass because single-file selections retain the archive-path label semantics)
- [x] Add per-entity graph visibility controls omitted by the broad filters task. (adds pure src/core/graph/visibility.ts exporting `VisibilityState`, `createEmptyVisibility`, `toggleHiddenNode` / `hideNodes` / `showNodes` / `restoreNodes` (all immutable reducers; `restoreNodes(graph, state, ids)` un-hides explicit deny-list entries AND clears `isolatedFocus` when any target id is currently isolation-hidden so users can actually recover isolation-implicit hides), `resetVisibility`, `isolateNeighborhood` / `clearIsolation`, and `applyVisibility(graph, state): ApplyVisibilityResult` — every reducer returns a fresh Set so callers can freely mutate the result without corrupting the input; `applyVisibility` runs a two-pass filter: (1) if `isolatedFocus` is set, delegate to `pruneNeighborhood` for the candidate id set (a missing focus id gracefully falls back to the full node set instead of blanking the graph), (2) drop every id in `hiddenNodeIds` even when it survived isolation, then drop every edge whose endpoints aren't both visible so no dangling handles reach the renderer; returns `counts: {visibleNodes, totalNodes, visibleEdges, totalEdges, hiddenNodeCount}` plus an `effectivelyHidden: ReadonlySet<string>` so a UI can enumerate every currently-hidden id (including isolation-implicit hides, not just the explicit deny-list); wired through src/core/graph/index.ts barrel; adds src/ui/components/TopologyVisibilityPanel.tsx — controlled bar rendering `visibleN/totalN nodes · visibleE/totalE edges · X hidden` counts, `Hide selected` / `Show only selected + neighborhood` / `Clear isolation` / `Reset all` action buttons keyed to the canvas selection, a per-hidden-id pill list (derived from `effectivelyHidden` so isolation-implicit hides show up too) whose click-to-restore button calls `restoreNodes` so isolation-hidden entries actually come back (not the old `showNodes` no-op that only touched `hiddenNodeIds`), and a searchable checkbox list of every queue+exchange in the graph (label + id substring, case-insensitive, capped at 100 with a `…N more` hint; checkbox state derives from `effectivelyHidden` and toggling an isolation-hidden entry restores it via `restoreNodes` so the UI can't misrepresent isolation-hidden nodes as "visible") so users can hide individual entities without going through the canvas; every interaction calls `onChange` with a fresh `VisibilityState`, never mutating the input; wired into TopologyGraphCanvas.tsx which now holds a `visibility` state, runs the graph through `applyVisibility` synchronously on the main thread (cheap O(V+E), and needs to react to checkbox toggles instantly), and feeds `visible.nodes`/`visible.edges` into `toReactFlowElements`; **the panel receives the post-filter, pre-visibility `graph` (not `rawGraph`)** so its searchable list, `effectivelyHidden` pill list, and `restoreNodes` isolation-membership check all run against the exact same node set the visibility overlay operates on — broadly-filtered entities cannot appear checked/visible in the searchable list, and isolation restore always agrees with what the canvas renders; 13 core tests in test/core/graph/visibility.test.ts cover immutability of every reducer (toggle/hide/show/reset/isolate/clearIsolation), applyVisibility no-op, individual hide dropping the node + every incident edge, restore round-trip, depth-1 neighborhood isolation surfacing structural ancestry, hidden ids respected inside isolation, missing-focus graceful fallback, count / effectivelyHidden reporting, and `restoreNodes` both leaving isolation untouched for in-neighborhood ids AND clearing `isolatedFocus` when the target was isolation-hidden; 11 UI regressions in test/ui/components/TopologyVisibilityPanel.test.tsx cover counts summary (with/without hidden badge), **mandatory** individual hide via checkbox toggle, hidden-item pill list with per-id restore, `Reset all` emitting an empty state, `Show only selected + neighborhood` disabled without a selection AND emitting the correct isolation state with one, `Clear isolation` visibility + effect, case-insensitive search filter, `Hide selected` disabled/enabled state, and **mandatory** isolation-hidden restore regressions: pill list surfaces isolation-hidden nodes with unchecked searchable checkboxes, pill click clears isolation and un-hides the node, and searchable-checkbox click on an isolation-hidden node also clears isolation; test/ui/components/topologyGraphCanvasPipeline.test.ts gains 2 canvas-composition regressions proving broad host filter + hide/restore + neighborhood isolation compose correctly — filter-removed nodes never appear in `effectivelyHidden` (so the panel can't offer a phantom restore), hide/restore on a survivor round-trips against the filtered graph, isolation-hidden ids in `effectivelyHidden` restore against the filtered graph and correctly clear isolation, and the broad-filter-removed host:b entities stay gone through the whole overlay pipeline)
- [x] Add bulk deselection for filtered entities in the Visibility panel. After the user enters a non-empty, trimmed search term, provide a single clear action such as `Hide all matches` / `Deselect all matches` that hides every matching queue and exchange in one operation—not only the first 100 currently rendered by the capped checkbox list. Matching must remain case-insensitive across entity labels and ids, preserve all non-matching visibility choices, use the existing immutable visibility reducers/state model, and compose correctly with broad graph filters and neighborhood isolation. Disable the bulk action when the normalized search is empty to prevent accidental hide-all. Add UI and visibility/pipeline regressions proving one click hides all matches (including a >100-match case), leaves non-matches unchanged, does not mutate the input state, and behaves correctly while isolation is active. (extends src/ui/components/TopologyVisibilityPanel.tsx with a memoized `normalizedQuery = query.trim().toLowerCase()` + `hasActiveQuery` flag reused by both filtering and the bulk buttons so whitespace-only queries never activate the bulk actions; adds `onHideAllMatches` / `onShowAllMatches` handlers that (a) no-op unless `hasActiveQuery` is true — belt-and-braces on top of the disabled state so a rogue click that bypasses the `disabled` attribute still can't fire, (b) operate on `filteredEntities.map(n => n.id)` which is the FULL match set derived from `searchableEntities` — NOT the 100-item capped render slice, so a "q.bulk" search hides all 150 in one call, (c) call the existing immutable `hideNodes(state, ids)` reducer that layers matches on top of any existing hide state without mutating input and without touching `isolatedFocus`, and (d) route the show path through `restoreNodes(graph, state, ids)` so isolation-hidden matches actually come back (clearing `isolatedFocus` when needed, matching per-pill Show semantics); adds two new buttons `[data-testid=topology-visibility-hide-all-matches]` and `[data-testid=topology-visibility-show-all-matches]` in a new search-row alongside the input — the Hide button surfaces the live match count in its label (e.g. `Hide all matches (150)`) so operators see what one click will affect BEFORE they take it, both are disabled while `hasActiveQuery` is false, and titles explain the disabled state; test/ui/components/TopologyVisibilityPanel.test.tsx adds a `largeGraphFixture(matchCount, distractorCount)` helper that generates >100 matching queues plus a handful of distractors and 7 mandatory bulk-action regressions covering: buttons disabled with empty AND whitespace-only search, single-click Hide-all-matches hides every case-insensitive label/id match while leaving non-matches visible and without setting isolatedFocus, preserves earlier explicit non-match hides, composes with active isolation (isolatedFocus preserved, matches added to deny-list on top), one click hides all 150 matches in the >100-fixture even though only 100 checkboxes render (with the button counter surfacing the true count), Show-all-matches restores explicit hides without mutating the input state, and Show-all-matches with isolation active clears `isolatedFocus` so isolation-hidden matches actually re-render; test/ui/components/topologyGraphCanvasPipeline.test.ts adds 2 pipeline regressions proving the bulk-hide state composes end-to-end through `applyGraphFilters → hideNodes → applyVisibility` — broad host filter + bulk-hide-by-substring drops every match from the rendered graph while leaving non-match survivors visible, filter-removed nodes stay gone, and no dangling edges reach the renderer; a second pipeline test pins that layering bulk-hide over an existing `isolatedFocus` preserves the isolation focus and applies both isolation + explicit deny-list to the filtered graph)
- [x] Visualize configured message-flow direction through shovels across hosts/namespaces and vhosts. Render a clearly directional shovel path from resolved source queue/exchange through the shovel to resolved destination queue/exchange, retain host/vhost boundary context, and animate particles/dashes on shovel edges with pause/reduce-motion controls. Label this as configured-flow visualization—not live message telemetry. If optional live rates are later supplied from RabbitMQ Management API data, scale animation by the supplied rate and show timestamp/staleness; never imply live traffic when only definitions JSON is loaded. Add cross-host and same-host/cross-vhost fixtures plus rendering tests proving direction, boundary labels, pause/reduced-motion behavior, and no credential leakage. (extends src/core/model/graph.ts with `LinkFlow` + `LinkFlowBoundary` types and a new optional `flow?: LinkFlow` field on `GraphEdge` — populated only for `shovels` / `federates` edges — carrying `{linkKind, linkName, role: "in"|"out", boundary: "same-vhost"|"cross-vhost-same-host"|"cross-host", sourceHostName, sourceVhostName, destinationHostName, destinationVhostName}` and documenting inline that the metadata describes *configured* topology direction, NOT live message telemetry; adds pure src/core/graph/shovelFlow.ts exporting `resolveEndpointNames` (fills the link's own host/vhost when an `EndpointRef` omits them; treats whitespace-only fields as absent), `classifyBoundary` (case-insensitive host equality catches Rabbit-A vs rabbit-a casing drift; case-sensitive vhost equality matches RabbitMQ semantics), `buildLinkFlow` (one-shot builder for both `role="in"` and `role="out"` used by `buildGraph`), plus label helpers `describeBoundary` and `describeLinkFlow` (the latter prefixes every summary with the literal word `configured` so an operator can't confuse the label with live traffic); wired through src/core/graph/index.ts barrel; src/core/graph/buildGraph.ts now builds a `hostById` map plus a `linkContextForVhost` closure and attaches a fully-populated `flow` on every shovel-in / shovel-out / fed-in / fed-out edge — direction preserved via the existing from/to wiring (in-edges terminate at the link node, out-edges leave it); the `deepSanitize` gate still runs across every node/edge/diagnostic so a rogue `amqp[s]://…` embedded in an endpoint URI or link name is redacted before any `LinkFlow` field reaches the UI; src/ui/components/topologyGraphElements.ts extends `FlowEdge.data` with `flow?: LinkFlow` and reads `flow.boundary` to emit human labels like `shovel "a-to-b" · cross-host` and `federation "orders→audit" · cross-vhost, same host` (falling back to the link name / edge kind when `flow` is absent so existing tests still pass); adds a `configuredFlowMotion?: {paused?: boolean; reducedMotion?: boolean}` option — either flag suppresses the animated dashed stroke on shovel/federation edges while arrowheads remain so direction is still readable, matching the pause/reduce-motion requirement; adds src/ui/components/ConfiguredFlowLegend.tsx exporting `ConfiguredFlowLegend` (caption "Configured message flow · shovel & federation edges depict declared topology direction, **not live traffic**." + a state label + a pause/resume button) and `usePrefersReducedMotion` (SSR-safe hook that subscribes to the `(prefers-reduced-motion: reduce)` media query with both `addEventListener` and legacy `addListener` fallback so Safari + jsdom both work); the legend's reduced-motion state overrides the user pause toggle — the button is disabled and the state label reports "animation off (reduced motion)" so an accessibility preference always wins; wired into src/ui/components/TopologyGraphCanvas.tsx which now holds a `configuredFlowPaused` React state, consumes `usePrefersReducedMotion`, memoizes `{paused, reducedMotion}` and passes it into `toReactFlowElements` so the flow-edge animation reacts live to both toggles, and renders `<ConfiguredFlowLegend>` above the filter panel so operators see the "configured, not live" caption before they interpret the animation; test/core/graph/shovelFlow.test.ts adds 14 pure classifier tests covering endpoint-name defaults (present, omitted, whitespace-only), boundary classification for same-vhost / cross-vhost-same-host / cross-host with case-drift, `buildLinkFlow` populating both roles, `describeBoundary` / `describeLinkFlow` stability + the mandatory "configured" prefix, and integration fixtures for a cross-host shovel (host names + role wiring + direction), a same-host cross-vhost federation, and a **credential-leakage regression** using a shovel `EndpointRef.uri = amqps://<user>:<pass>@rabbit-a:5671/` asserting the serialized graph contains neither user nor password nor the `pass@rabbit-a` combination even though the URI carries userinfo; test/ui/components/topologyGraphElements.test.ts adds 6 rendering regressions on a shared cross-host + cross-vhost scenario proving boundary-annotated labels for both shovel and federation edges, direction preservation via source/target endpoints, `flow` piped through `FlowEdge.data`, default-on animation, user pause suppresses animation while arrowheads survive, and reduced-motion suppresses animation independently of pause; test/ui/components/ConfiguredFlowLegend.test.tsx adds 6 jsdom regressions covering the "configured, not live" caption, pause/resume button toggle + label swap, and reduced-motion disabling the button + overriding the state label regardless of the pause flag)
- [x] Investigate and fix the failed import of `rabbit_ffm-sbptrd01m03_2026-7-27`, with explicit compatibility across RabbitMQ definition-export versions. Reproduce the failure using the exact supplied artifact and trace the complete picker/drop → byte loading → archive/JSON sniffing → definitions parser → worker response → graph-build/UI pipeline to identify whether the break is caused by filename/extension handling, top-level export shape, renamed or newly nested fields, missing/null values, version-specific value types, unsupported entity variants, resource limits, or swallowed diagnostics. Treat RabbitMQ exports as versioned, schema-drifting input: tolerate unknown additive fields, normalize known structural variants through explicit adapters/capability detection instead of assuming one RabbitMQ version, preserve supported entities when only part of an export is unfamiliar, and emit actionable source-scoped diagnostics for unsupported structures rather than failing silently or blanking the application. Do not commit the raw production export or credentials; derive a minimal sanitized fixture that preserves the failing structure and prove its serialized parsed/graph output contains no AMQP credentials or other secrets. Add parser, worker/client, ImportPanel upload/drop, and end-to-end graph regressions for the sanitized failing fixture plus representative older/current export shapes, while preserving all existing definitions and split-management-dump behavior. Initial reproduction with the privately supplied RabbitMQ 3.12.6 export (kept outside Git) proves the file-picker and core import complete—2 vhosts, 94 exchanges, 300 queues, 547 bindings, 68 shovels, and a 481-node graph—but exposes a concrete schema mismatch: all 68 shovels use an array-valued `src-uri`, 67 also use an array-valued `dest-uri`, while `parseShovel` currently accepts only a single string via `asString`. The URI lists are therefore discarded for endpoint resolution, losing remote host/vhost context and potentially misconnecting shovel paths even though import reports success. Prioritize safe normalization of string-or-array HA URI forms, retain deterministic/redacted endpoint alternatives without credentials, and add diagnostics for empty or mixed-invalid URI lists. (partial: parser sub-scope only — extends src/core/parse/runtimeParameters.ts with a `pickShovelUri` helper wired into `parseShovel` for BOTH `src-uri`/`src_uri` and `dest-uri`/`dest_uri` — accepts string OR non-empty array of strings, TRIMS strings before selection so whitespace-only scalar/list entries behave like the "no URI" case rather than being silently passed to the URI parser, treats the first trimmed non-empty string as the primary endpoint so host/vhost context is derived from the same URI RabbitMQ tries first, threads through the existing `endpointFromShovelSide → parseAmqpUri → redactAmqpUri` pipeline; alternate URIs stay preserved DETERMINISTICALLY and REDACTED in `Shovel.arguments` via the existing `redactValue(v)` → `redactUnknown` recursion (arrays walked, every `amqps?://` string rewritten via `redactAmqpUri`); three source-scoped diagnostic codes cover the observed failure modes without silently discarding shovels: `runtime-params.shovel-uri-empty-list` (warning) for `[]`, `runtime-params.shovel-uri-invalid-list` (warning) when every array entry is non-string / empty / whitespace-only, `runtime-params.shovel-uri-mixed-list` (info, message reports the skipped count and clarifies "non-string OR empty/whitespace") when some entries are unusable but at least one URI remains; 7 new tests in test/core/parse/runtimeParameters.test.ts pin: array-valued `src-uri`+`dest-uri` picks the first URI per side and preserves the full redacted array in `arguments["src-uri"]` (with `USERNAME_PLACEHOLDER`/`PASSWORD_PLACEHOLDER` guaranteed absent from `JSON.stringify(shovel)`), empty-array `src-uri` still materialises a queue-only shovel + `shovel-uri-empty-list`, all-invalid `src-uri` (null/number/nested-object) surfaces `shovel-uri-invalid-list` with no URI-derived host, mixed `dest-uri` picks the first valid entry and reports `shovel-uri-mixed-list`, whitespace-only scalar `src-uri` is treated as no URI with NO spurious mixed/invalid diagnostic, whitespace-only array entries are counted as skipped in the mixed-list message and the first non-whitespace URI wins, and an all-whitespace array raises `shovel-uri-invalid-list`. Sanitized failing-fixture regression added: test/fixtures/rabbit-3.12-shovel-ha-uri.json reproduces the RabbitMQ 3.12.6 export shape — `parameters[*].value["src-uri"]`/`["dest-uri"]` as JSON arrays (HA form) plus a mixed shovel with scalar src + single-entry dest array, every URI `amqp://REDACTED@*.example.internal` so no credentials or real infra names ship; new test/fixtures/rabbit312ShovelHaUriFixture.test.ts drives it end-to-end through `parseDefinitionsExport → parseRuntimeParameters → buildGraph` (9 tests) and pins: no error-severity diagnostics from either parser on the 3.12.6 payload, both shovel raw parameters preserved verbatim, primary source/destination endpoints derived from the FIRST URI in each HA array, alternate URIs retained (redacted) in `Shovel.arguments["src-uri"]`/`["dest-uri"]`, mixed shape (scalar + single-entry array) still resolves without regression, no spurious `shovel-uri-empty-list`/`invalid-list`/`mixed-list` diagnostics on the clean payload, no `user:pass@` substring in `JSON.stringify(runtime.shovels)`, both shovel nodes materialise in the graph (regression pin against the pre-fix silently-dropped case), and no credential leak in the full node+edge serialization while sanitized `*.example.internal` hostnames DO survive so the graph still encodes remote endpoint context; fixtures README updated to document the new fixture. Worker/client end-to-end regression added: appends a `handleImportArchiveMessage` regression to test/core/import/importArchiveWorker.test.ts driving the sanitized 3.12.6 HA-URI fixture bytes through the worker dispatcher and asserting: both shovels round-trip as `orders-shovel-ha` and `audit-shovel-mixed-shape`, the HA shovel's `source.host` and `destination.host` come from the first URI in each array even after crossing the worker message boundary, and the full serialized worker response contains no `user:pass@` AMQP userinfo pattern. ImportPanel upload/drop regressions added: two new jsdom tests in test/ui/components/ImportPanel.test.tsx exercise the file-picker AND drag-and-drop paths with the 3.12.6 HA-URI fixture, asserting `onImported` fires with both shovels intact, HA source/destination hosts derived from the first URI in each list, and no credential leak in `JSON.stringify(onImported call payload)`. Representative older/current export-shape fixtures added: test/fixtures/rabbit-3.8-legacy.json models the RabbitMQ 3.8.35 shape (scalar user `tags` string, scalar shovel `src-uri`/`dest-uri`, classic queues without an explicit `x-queue-type`) and test/fixtures/rabbit-4.0-current.json models the RabbitMQ 4.0.2 shape (rich vhost fields — `description`, `tags` array, `default_queue_type`, nested `metadata` — quorum queues via `arguments["x-queue-type"] = "quorum"`, and a shovel that mixes an HA-form `src-uri` array with a scalar `dest-uri` so the fix is exercised alongside the newer surface); test/fixtures/rabbitExportCompatibilityFixture.test.ts drives both fixtures through `parseDefinitionsExport → parseRuntimeParameters → buildGraph` in 8 tests: legacy shape parses with no error-severity diagnostics and enumerates both vhosts + exchanges + bindings, the legacy scalar-URI shovel resolves to redacted endpoints and materialises host/vhost/exchange/queue/shovel graph nodes, current 4.x shape emits no `definitions.vhost-*` diagnostics for additive vhost fields (positive tolerance pin), quorum queues survive round-trip with `x-queue-type: "quorum"` intact, the 4.x shovel's HA `src-uri` picks the first URI while the scalar `dest-uri` still resolves, alternate HA URIs stay redacted in `Shovel.arguments["src-uri"]`, and the resulting graph carries `current-primary.example.internal` context while `JSON.stringify({nodes,edges})` still contains zero `user:pass@` substrings; fixtures README updated to document both new shapes; full `scripts/verify.sh` — vitest + `tsc -b --noEmit` + `vite build` — is green with 569/569 tests passing across 51 files.)
- [x] Add file/RAR/zip import panel. (adds `jszip` runtime dep, framework-agnostic driver src/core/import/importArchive.ts exporting `importTopologyArchive` + `ImportResult` + `ImportedFile` — sniffs archive kind from filename extension or magic bytes (`Rar!` / `PK\x03\x04`), iterates entries, and runs a **two-phase importer**: definitions files parse eagerly per-file; management-dump files (`queues.json`/`exchanges.json`/`bindings.json`/`parameters.json`/`policies.json`/`vhosts.json`) are staged by host (path-hint `hosts/<name>` first, filename prefix fallback that **skips canonical export names like `queues.json`/`exchanges.json`/`bindings.json`/`parameters.json`/`policies.json`/`vhosts.json`/`definitions.json`** so root-level split dumps land in one shared `__unknown_host__` bucket instead of splintering into pseudo-hosts named after the file kind) and then group-parsed via a single `parseSplitManagementDump` call per host so cross-file binding + runtime-parameter resolution works; canonical root-level filenames like `queues.json`, `bindings.json`, and `definitions.json` are treated as dump/export shapes rather than fake host names; aggregated parse+runtime is attached to only the first dump entry per host to avoid inflating per-file totals; non-JSON entries in archives are recorded but skipped; enforces resource limits (`IMPORT_DEFAULT_LIMITS`: 50 MB compressed archive, 5 000 entries, 25 MB per entry, 100 MB total decompressed): ZIP performs an all-header declared-size/count preflight, then extracts with JSZip `internalStream("uint8array")`, pausing as soon as an actual per-entry or remaining-total cap is crossed and retaining no more than the bounded entry buffer plus one emitted chunk; declared and actual totals are tracked independently (never double-counted), while every produced chunk—including output from per-entry-skipped forged entries—counts toward the terminal actual total; header-oversized entries are warned/skipped so later valid entries continue; the streaming `end` handler resolves a `pendingLimitDecision` before any success/failure branch so a synchronous JSZip `end` cannot race the microtask-scheduled limit resolution and return a truncated `"complete"` payload — regression-pinned via a rigged `internalStream` that emits an oversized chunk followed synchronously by `end`, asserting the pending `entry-exceeded` decision wins and no truncated bomb entry leaks through; RAR applies terminal all-header entry-count/cumulative preflight, excludes selected header-oversized files before node-unrar-js extraction, warns/skips actual per-entry excess while counting it toward the terminal actual total, and discards results on terminal extraction failure — the inline `files.length = 0` clear at the `rar.total-size-limit-exceeded` failure site is regression-pinned in `test/core/parse/rarLoader.limits.test.ts` via a mocked `node-unrar-js` extractor whose headers understate `unpSize` so preflight admits both entries while actual extraction crosses `maxTotalBytes` on the second entry, asserting both the terminal diagnostic and an empty file list; adds React panel src/ui/components/ImportPanel.tsx with file-picker + drag-and-drop, loading/error states, and a live totals + diagnostics summary; wired into src/App.tsx; importer tests include explicit RAR regressions proving custom per-entry limits skip only oversized selected headers while cumulative-size limits fail terminally in RAR preflight (`rar.entry-too-large` / `rar.total-size-limit-exceeded`); adds `jsdom` + `@testing-library/react` devDeps and 5 ImportPanel component tests in test/ui/components/ImportPanel.test.tsx covering initial render, file-picker success + summary + `onImported` callback, malformed-JSON error surfaced in summary, drag-and-drop path parity, and drag-active style toggling; vite.config.ts uses `environmentMatchGlobs` so only `test/ui/**` runs under jsdom)
- [x] Add topology summary and diagnostics panel. (adds src/ui/components/TopologySummaryPanel.tsx exporting `TopologySummaryPanel` + `summarizeImportedTotals` — reduces an `ImportResult` to a two-column overview (Files: total entries / definitions / management-dump / non-JSON / load errors; Topology: hosts (de-duplicated by host id) / vhosts / exchanges / queues / bindings / shovels / federation links / policies / runtime parameters) with an archive-kind badge, plus a diagnostics section that renders coloured severity counts in the heading (`data-testid="diagnostics-total"`) and a collapsible details block (`data-testid="diagnostics-toggle"` / `diagnostics-details`) containing counts-by-code and per-severity `<details>` lists — error block auto-opens, warning/info collapsed — each severity list capped at `maxDiagnosticsPerSeverity` (default 25) with a hidden-remainder line; ImportPanel now renders a completion notice + delegates the summary to TopologySummaryPanel so the existing ImportPanel component tests (backwards-compat assertions on `/Loaded JSON/i` + `Exchanges: 5` + `Queues: 4` + `Diagnostics:` counts) still pass while the richer diagnostics view is available for every archive kind; 5 new jsdom component tests in test/ui/components/TopologySummaryPanel.test.tsx covering totals rendering + archive-kind badge, host de-duplication across parsed files sharing a host id, "No diagnostics" clean-state (no toggle button), diagnostics severity counts + expand/collapse + per-code details, and `maxDiagnosticsPerSeverity` cap with hidden-remainder message)
- [x] Add search box for queue/exchange names. (adds src/core/import/aggregate.ts exporting `aggregateImportedTopology` — flattens an `ImportResult` into a single `BuildIndexesInput`, merging hosts/vhosts/exchanges/queues/policies from every parsed file plus shovels/federations from every runtime file, de-duplicating by canonical id on first-seen so the same host or entity referenced by multiple files never appears twice in the index; wired through src/core/import/index.ts barrel; adds src/ui/components/EntitySearchBox.tsx rendering a controlled search input (`data-testid="entity-search-query"`) + kind selector (`data-testid="entity-search-kind"` with either/exchange/queue), memoizes `buildTopologyIndexes(aggregateImportedTopology(result))` so the index is rebuilt only when the ImportResult identity changes and not on every keystroke, then combines `findEntity` (elevated exact block, `data-testid="entity-search-exact"`) with `fuzzyFindEntity` (secondary fuzzy block, `data-testid="entity-search-fuzzy"`) — the fuzzy list filters out any entity already surfaced in the exact block so no result appears twice; idle-state hint, empty-state hint, and per-row `onSelect` callback via button rows carrying `data-testid="entity-search-result-<id>"`; wired into src/App.tsx which now holds the ImportResult in state and renders `EntitySearchBox` beneath the ImportPanel/TopologySummaryPanel once an import completes; 3 aggregate tests in test/core/import/aggregate.test.ts covering multi-file merge + id de-dup, runtime shovel/federation aggregation, and empty-input safety; 6 EntitySearchBox jsdom tests in test/ui/components/EntitySearchBox.test.tsx covering initial render, exact-elevation + no-overlap-with-fuzzy for a name shared across a queue and an exchange, fuzzy-only substring results when no exact match, kind=queue filtering out exchanges, empty-state, and onSelect callback wiring)
- [ ] Add a name-driven focused flow explorer that builds the complete graph to and from a selected queue or exchange across all loaded definitions. Wire the existing `EntitySearchBox` selection into `TopologyGraphCanvas` instead of leaving `onSelect` disconnected: after the user enters a queue or exchange name, resolve exact/fuzzy candidates from the aggregate of every loaded definitions/split-dump file, require an explicit host/vhost choice when names are ambiguous, then switch the canvas into a focused mode containing the selected entity plus its complete reachable incoming and outgoing message-flow chain. Reuse the bidirectional traversal/highlight engine—do not create a second traversal implementation—and include exchange-to-exchange bindings, queue bindings, alternate exchanges, configured shovels and federation links (including cross-host/cross-vhost and external endpoints), with dead-letter edges following the existing opt-in setting; exclude structural `contains` edges from traversal while retaining only the minimal host/vhost context needed to understand placement. The focused graph must include every branch and connecting relation within the current depth limit, remain cycle-safe, fit the resulting graph into view, clearly identify the selected target and direction of flow, and provide a one-click `Clear focus / Show full topology` action. It must aggregate cross-file topology before resolving/traversing, never choose an arbitrary duplicate name, remain responsive via the existing worker path with stale-response protection, compose with filters and visibility without resurrecting excluded nodes, and surface actionable empty/truncated/unresolved-link states. Add search-selection, ambiguity/disambiguation, aggregate cross-file, queue/exchange, shovel-chain, branching/cycle/depth, worker-staleness, filter/visibility, reset, and end-to-end canvas regressions. (partial: search-selection wiring only — hoists `selectedNodeId` state into src/App.tsx and threads it through `TopologyGraphCanvas` via new controlled-selection props (`selectedNodeId` + `onSelectionChange`). Controlled mode is detected from PROP PRESENCE via `Object.prototype.hasOwnProperty.call(props, "selectedNodeId")` *before* destructuring, matching React's own controlled-input pattern — passing `selectedNodeId={undefined}` still counts as controlled; passing only `onSelectionChange` leaves the canvas uncontrolled; passing only `selectedNodeId` still switches to controlled mode and canvas-initiated changes silently no-op (documented footgun). When controlled, the prop is authoritative and internal state is bypassed entirely; otherwise the internal `useState` continues to drive the selection so pre-existing uncontrolled callers keep working. App.tsx handles `EntitySearchBox.onSelect` by setting the hoisted state to `entity.id` — so clicking a search result lights up the same upstream-highlight + details + path-explanation UI that a canvas click would — and resets the hoisted state on a fresh import so a stale node id cannot linger against a newly built graph. 4 jsdom tests in test/ui/App.test.tsx pin the App-level wiring (mocking `ImportPanel` + `TopologyGraphCanvas` with lightweight stubs to avoid ReactFlow/worker weight): initial `selectedNodeId` is empty after import, clicking a queue exact-match search result flows the entity id into the canvas prop, canvas-initiated `onSelectionChange(undefined)` clears the state and a subsequent exchange search selection still works, and re-triggering an import invalidates the previously selected node. 7 canvas-level jsdom tests in test/ui/components/TopologyGraphCanvas.test.tsx mock only `reactflow` (via `vi.mock` — surfaces `onNodeClick` + `onPaneClick` as click-target buttons on a queue node in the fixture) and drive the real canvas with a mocked worker client: controlled mode surfaces the selection summary from the prop alone, "Clear selection" in controlled mode calls `onSelectionChange(undefined)` without touching internal state, a node-click in controlled mode calls `onSelectionChange("queue:h:q")` and the summary bar stays hidden because the parent hasn't propagated the change (proving internal state was NOT flipped), the presence-of-prop regression pin — `selectedNodeId` alone (no callback) still switches to controlled mode and canvas clicks silently no-op while the prop-driven summary stays visible — and the uncontrolled fallback trio: no summary at idle, node click flips internal state and shows the summary, pane click clears the internal selection. Focused-mode graph clipping added: TopologyGraphCanvas now takes `focusNodeId` + `onFocusChange` + `focusMaxDepth` props. When `focusNodeId` is set, the canvas wraps its post-visibility graph with `pruneNeighborhood` (direction `"both"`, structural `contains` ancestry preserved) so only the routing-edge chain reachable from the focus node is rendered — the clip runs AFTER the filter and visibility passes so a focused view CANNOT re-render excluded entities structurally (dedicated composition regressions are still open — see TODO below — the placement of the clip in the pipeline order is what enforces this, not a jsdom assertion). A new focused-mode banner renders above the graph with `Focused on <id>: N nodes, M edges (truncated at max depth)` OR `Focus target '<id>' is not in the current graph — showing an empty focused view.` (surfacing the actionable empty state without throwing or rendering a stale full graph), plus a `Show full topology` button (`data-testid="topology-graph-clear-focus"`) that calls `onFocusChange(undefined)`. When `onFocusChange` is omitted the banner still renders but the button is hidden so a fully-controlled parent keeps sole ownership of when to exit focused mode. Fit-to-view wiring: the canvas captures the `ReactFlowInstance` via `onInit` and calls `instance.fitView({ duration: 0 })` from a `useEffect` keyed on the memoised `focused` value — so activating focused mode, switching to a different focus target, and clearing focused mode each refit the viewport onto the newly rendered subgraph (task requires "fit the resulting graph into view"). 6 new jsdom tests in test/ui/components/TopologyGraphCanvas.test.tsx pin the focused-mode behavior: no banner without `focusNodeId`; focused mode clips the rendered graph to EXACT counts — expanded fixture adds an unrelated `exchange:h:unrelated → queue:h:unrelated` chain plus `contains` edges, baseline unfocused render captures 6 nodes / 2 non-contains edges from the mocked ReactFlow's recorded props, focused render captures exactly 4 nodes (`{queue:h:q, exchange:h:x, vhost:h:/, host:h}` — sorted-id assertion) and exactly 1 non-contains edge (`b:x->q`) with the banner reporting 4 nodes / 4 edges (subgraph total including 3 surviving contains); `Show full topology` calls `onFocusChange(undefined)`; a missing focus target surfaces the empty-view banner without throwing; omitting `onFocusChange` keeps the banner visible while hiding the clear button; and a fit-to-view regression asserts the mocked ReactFlow's `fitView` spy is called MORE times after activating focused mode, after switching to a different focus target, and after clearing focused mode. Filter/visibility composition regressions for focused mode added: extracted the canvas's post-filter render pipeline into a new pure module src/ui/components/topologyRenderPipeline.ts exporting `composeFocusedTopology({ graph, visibility, focusNodeId, focusMaxDepth }) → { visible, focused, renderInput }` — TopologyGraphCanvas now calls this function instead of inlining `applyVisibility → pruneNeighborhood → renderInput` selection, so reordering or bypassing a stage in the canvas wiring breaks a shared-function assertion instead of silently passing. 5 pipeline tests in test/ui/components/topologyGraphCanvasPipeline.test.ts invoke `composeFocusedTopology` directly (same function TopologyGraphCanvas calls) and pin: focused view on the surviving host reaches its full chain but does NOT resurrect the filtered-out `host:b` / `queue:b:q2` entities; focusing on a filter-excluded target yields `focused.focusMissing: true` with empty nodes/edges (actionable empty state); visibility-hiding a mid-chain exchange (`exchange:a:x2`) BEFORE the focused-mode clip cuts the chain and prunes the now-unreachable upstream ancestor (`exchange:a:x1`) — proving the clip sees the visibility overlay, not the raw graph; the end-to-end pipeline (filter + visibility hide + focus + render) asserts no rendered node id belongs to any upstream-excluded id and every rendered edge connects two kept nodes; and an identity-check regression proves `composition.renderInput === composition.focused` when focus is active (so any wiring that hands the pre-focus `visible` payload to the renderer while focus is on fails immediately), while unfocused compositions return `renderInput.nodes === visible.nodes`. Shovel-chain end-to-end canvas regressions added: extended test/ui/components/topologyGraphCanvasPipeline.test.ts with a new `shovelChainFixture()` that layers a cross-host shovel (`exchange:a:x3 → shovel:a:s1 → queue:b:q2`) on top of the shared `pipelineFixture`, then adds 4 tests that invoke `composeFocusedTopology` (the exact function TopologyGraphCanvas uses) so a shovel-chain wiring regression in the canvas breaks a shared-function assertion: focusing on the downstream queue walks the full chain upstream (`queue:b:q2 ← shovel:a:s1 ← exchange:a:x3 ← exchange:a:x2 ← exchange:a:x1`) with contains ancestry preserved for BOTH participating hosts + vhosts and both `shovels` edges surviving; focusing on a middle exchange (`exchange:a:x2`) reaches BOTH upstream `x1` AND downstream `x3 → q1` PLUS the shovel branch `x3 → shovel → queue:b:q2` — proving `direction: "both"`; focusing on the shovel node itself walks the incoming exchange chain and the outgoing destination queue; depth-truncation regression pins `focusMaxDepth=1` cuts the chain to the shovel's immediate neighbors (`exchange:a:x3` + `queue:b:q2` + focus) with `focused.truncated=true`. Every focused edge routes only between surviving nodes so React Flow never sees a dangling handle. Branching + reset end-to-end regressions added: 2 additional shovel-chain-fixture tests in test/ui/components/topologyGraphCanvasPipeline.test.ts drive `composeFocusedTopology` and pin: focusing on `exchange:a:x3` after extending the fixture with a second downstream queue (`queue:a:branch`) preserves ALL THREE downstream branches (`queue:a:q1` original + `queue:a:branch` fan-out + `shovel:a:s1 → queue:b:q2`) with every corresponding routing edge in the focused edge set — a wiring bug that followed only the first outgoing edge would drop branches; the reset regression composes twice — first with `focusNodeId` set (rendering a clipped subgraph), then without (mimicking the canvas's `Show full topology` → `onFocusChange(undefined)` transition) — and asserts the second call's `renderInput` re-contains every original node and identity-equals `visible.nodes`/`visible.edges` so a stale focused subgraph cannot linger after focus is cleared. Still TODO: host/vhost ambiguity disambiguation UI, cross-file aggregate resolution for search selection (currently the App layer just passes `entity.id` through), truncated-mode indicator on the search selection, and worker-path staleness protection regressions for search-driven selections — task box stays unchecked.)
- [x] Add graph canvas with React Flow. (adds `reactflow` runtime dep, extends `aggregateImportedTopology` to carry per-file bindings de-duplicated by id so `AggregatedTopology extends BuildIndexesInput` with a `bindings` array feeds both `buildTopologyIndexes` and `buildGraph`; adds pure src/ui/components/topologyGraphElements.ts exporting `toReactFlowElements`, `FlowNode`, `FlowEdge` — trivial column-per-kind layout in `host → vhost → external → exchange → shovel/federation → queue` order with vertical stacking inside each column, per-kind background/border palette, per-edge-kind stroke styling, animated shovel/federation edges, binding routing keys inlined into edge labels, and defensive drop of edges whose endpoints aren't in the node set so React Flow never sees a dangling handle; adds src/ui/components/TopologyGraphCanvas.tsx wrapping `ReactFlow` with `Background`/`Controls`/`MiniMap`, a `contains`-edge toggle checkbox, and a live `N nodes · M edges` stats line; wired into src/App.tsx beneath the search box; adds 5 pure-function tests in test/ui/components/topologyGraphElements.test.ts covering column-and-row layout, `contains`-toggle behaviour, missing-endpoint edge drop, routing-key rendering, and shovel/federation animation; adds a bindings-aggregation regression to test/core/import/aggregate.test.ts and updates the empty-input assertion to include the new `bindings: []` array)
- [x] Add custom node/edge styling by entity/flow type. (extends src/ui/components/topologyGraphElements.ts with per-entity refined styling: exchanges get an AMQP-type sub-palette (topic/direct/fanout/headers) plus a neutral "other" fallback for plugin types (consistent-hash, x-random, x-delayed-message, …), queues get an `x-queue-type` sub-palette (classic/quorum/stream) with a neutral "other" fallback, non-durable queues render with a dashed border and a `[transient]` badge; adds `FlowNodeData.flowType` (`exchange:<type>` | `queue:<type>` | `host|vhost|external|shovel|federation`) plus `subtypeBadge` so downstream filter UIs can select by AMQP subtype without re-deriving from raw entity data; every rendered edge now carries a `markerEnd` arrowhead in the edge's stroke colour, and edge stroke widths/dash patterns are tuned per kind (thicker shovels/federates, dashed alternate-exchange/dead-letter/contains); 3 new pure-function tests in test/ui/components/topologyGraphElements.test.ts cover exchange-type styling (distinct backgrounds + badge + unknown-type flow-type passthrough), queue-type + durability styling (badge composition + dashed transient border), and arrowhead marker colour matching the edge stroke)
- [x] Make each queue, exchange, and shovel node's vhost immediately distinguishable on the graph. (extends src/ui/components/topologyGraphElements.ts with `VhostContext` (`vhostId`, `vhostName`, `hostId`, `hostName`, `hostDiscriminator`, `isDefault`, `ambiguous`, `unknown`) and adds `FlowNodeData.vhostContext` + `vhostBadge` + `vhostTooltip`; a new `buildVhostLookup` pass over `graph.nodes` collects vhost → {name, hostId} and host → {name} strictly from canonical `Vhost` / `Host` data on `node.data.name` — display `node.label` is never used as a fallback so the resolver operates on the canonical model, not on the display layer — tracks vhost names shared across >1 host, and marks those as `ambiguous`; also tracks `duplicateHostNames` — host names that map to more than one distinct `hostId` — so the badge never relies on a duplicated `Host.name` alone; per-node `resolveVhostContext` reads the canonical `data.vhostId` on queue/exchange/shovel/federation nodes, resolves the human-readable vhost name from the lookup, falls back to an explicit `unknown vhost` context when `vhostId` is present but the vhost node is absent from the graph, ALSO emits the same `unknown` fallback (carrying the entity's `hostId`/`hostName` when available) when `vhostId` is missing entirely — so every queue/exchange/shovel/federation node renders a badge instead of silently omitting one — and maps `external` nodes to either their `EndpointRef.vhost` string or an explicit `unknown vhost` fallback (never leaks the raw uri); `pickHostDiscriminator` chooses the badge's host prefix stably: `hostName` when it maps to a unique `hostId`, otherwise the canonical `hostId` (guaranteed unique per host), otherwise a literal `unknown-host` — so ambiguous vhosts on two hosts that share a name still render with distinct badges; the compact badge renders as `<vhostName>` for uniquely-named vhosts, `<hostDiscriminator>/<vhostName>` for ambiguous vhost names OR unknown-vhost fallback when we have a host, and a bare `unknown vhost` when we have no host metadata at all, truncated at 24 chars via a collision-safe strategy: when the badge text exceeds the budget AND the context has a stable identity (vhostId, or the composite host+vhost identity for unresolved-vhost fallback contexts), the result is `<prefix>…<hash>` where `<hash>` is an FNV-1a hash of that identity, computed via a batch `computeVhostBadgeMap` pass with an **adaptive hash length** — starts at 4 hex (16 bits) so small graphs keep the compact `…[0-9a-f]{4}$` shape the tests pin, and if any two distinct disambiguators would produce the same truncated badge the whole batch retries at 5, 6, 7, 8 hex (full 32-bit FNV), with a final deterministic `-N` ordinal fallback for the astronomical 32-bit collision — guaranteeing that ANY topology, not just hand-picked inputs, produces unique badges across distinct entity identities. Regression pinned by a 3000-vhost same-host stress test that would collide under the old 16-bit-only path (birthday bound ≈ 256) and now passes because the batch adapts hash length to the graph's disambiguator cardinality. Two long badges that share a common prefix AND tail still render distinctly (`org_service_orderprocessing_pipeline_alpha_v1_release_20260831` vs `..._beta_v1_release_20260831` become `org_service_orderproc…<distinct-hash>` with different hashes); when no disambiguator is available, falls back to middle-out truncation (`<prefix>…<tail>`) so a shared leading prefix still exposes the discriminating tail; the hash is deterministic so React reconciliation doesn't churn across renders; the tooltip appends `on host <hostName>` (adding `(<hostId>)` when the host name is itself duplicated) or falls back to `on host <hostId>` when no name is available; the display label appends ` · <badge>` after any existing `[subtype]` prefix so both badges remain readable, and the full unabbreviated context stays in `vhostTooltip` for an accessible label; host/vhost nodes are skipped since they are the container themselves; highlight/target/off-path styling is preserved because the badge is data on the label string only. TopologyGraphCanvas registers a custom `topology` React Flow node type (`TopologyEntityNode`) via `nodeTypes` that renders `data.label` inside a `<div>` carrying a real `title` (populated from `data.vhostTooltip`) for the hover surface AND a composed `aria-label` of the form `"<visible label>, <vhostTooltip>"` — so screen readers announce entity identity FIRST (`[topic] x · /` retains the subtype badge + entity name) and then the disambiguating full-context announcement (`vhost / on host h`); a tooltip-only `aria-label` would override the visible label and hide entity identity. Host/vhost nodes have no vhost context so both `title` and `aria-label` are omitted and the a11y tree falls back to the visible label text. `FlowNode.type` is now the string literal `"topology"` so the canvas's `nodeTypes` map is the sole rendering path. 10 graph-element tests in test/ui/components/topologyGraphElements.test.ts cover the four entity kinds, host+vhost skip, ambiguous vhost names across hosts with host-prefixed badges + unambiguous baseline, long-name truncation with the tooltip carrying the full name AND the badge carrying a collision-safe `…<hash4>` suffix, two long vhost names sharing a common prefix on the same host produce distinct truncated badges, host-prefixed contexts sharing prefix + tail (ambiguous vhost on hosts named `rabbit-cluster-region-eu-alpha-primary` / `-beta-primary`) still render distinctly, and the stable-hash-suffix determinism regression proves two renders of the same input produce identical badges, external endpoint unknown/known cases, unresolved `vhostId` orphan surface, highlight-composition regression proving selection target ring + off-path dim keep their `vhostBadge`, PLUS three review-driven regressions: (i) duplicate host names — two hosts named `"rabbit"` sharing a vhost `"orders"` produce distinct badges `host:a/orders` vs `host:b/orders` and tooltips `on host rabbit (host:a)` vs `on host rabbit (host:b)`; (ii) missing host metadata — an orphan `hostId` with no loaded host node still surfaces in the badge/tooltip as `host:orphan/orders` / `on host host:orphan`; (iii) missing `vhostId` — a queue with `hostId` only renders `rabbit-a/unknown vhost`, a shovel with no host data renders the bare `unknown vhost` fallback, and duplicate `q1` entity names on two different hosts render with two distinct badges. 3 new canvas regressions in test/ui/components/TopologyGraphCanvas.test.tsx drive the real canvas through an extended ReactFlow mock that renders each node via the registered `nodeType` component: (a) accessibility — after the graph builds, the rendered `queue:h:q` and `exchange:h:x` DOM nodes both carry `title="vhost / on host h"` AND `aria-label="vhost / on host h"`, and the visible label includes the ` · /` suffix; (b) filter composition — flipping the entity-kind filter to allow only `host` removes every entity node from the render (no stale badge DOM lingers) while the surviving host node has no fabricated tooltip; (c) visibility/isolation composition — clicking the visibility panel's per-entity toggle for `queue:h:q` removes that node from the render while the sibling `exchange:h:x` keeps its full `title`+`aria-label` intact — proving the tooltip surface survives sibling drops; (d) TRUE isolation regression — selecting `queue:h:q` then clicking `topology-visibility-isolate-selected` prunes the unrelated queue+exchange chain (`queue:h:unrelated`, `exchange:h:unrelated`) from the render while the isolated queue keeps its full accessible name (`q · /, vhost / on host h`) and never degrades to `unknown vhost`, proving `contextNodes` (pre-filter/pre-visibility) is consulted even when isolation removes structural containers from the render graph. `toReactFlowElements` accepts an optional `contextNodes` (defaults to `graph.nodes`) that is used as the sole source-of-truth for `buildVhostLookup`; TopologyGraphCanvas passes the pre-filter `rawGraph.nodes` here so hiding a host/vhost container never degrades a still-visible entity's badge to `unknown vhost`. 3 additional pipeline regressions in test/ui/components/topologyGraphCanvasPipeline.test.ts + test/ui/components/topologyGraphElements.test.ts pin the fix: (i) element-level — supplying `contextNodes` while the render graph omits every host/vhost container keeps the queue's `vhostBadge`/`vhostTooltip` intact and `unknown: false`; (ii) pipeline visibility — hiding host + vhost via `hideNodes` while the queue survives produces `unknown vhost` WITHOUT `contextNodes` and the correct `a//` + `vhost / on host a` WITH `contextNodes` (documenting the good vs bad path in one test); (iii) pipeline filter — allow-listing only `entityKinds:{queue}` drops every host/vhost/exchange from the render graph while the surviving `queue:a:q1` and `queue:b:q2` keep their per-host discriminated badges (`a//` vs `b//`), and no excluded container id reappears via the render pipeline. Same-canvas filter regression in test/ui/components/TopologyGraphCanvas.test.tsx now flips the entity-kind filter to `queue`, asserts the host/vhost DOM nodes disappear from the ReactFlow mock, and asserts the surviving queue's rendered node carries `title="vhost / on host h"`, `aria-label="vhost / on host h"`, and `· /` in its visible label — proving canvas wiring keeps canonical context through the filter pipeline.)
- [x] Highlight upstream paths for selected queue/exchange. (adds src/core/graph/upstreamHighlight.ts exporting `computeUpstreamHighlight` + `UpstreamHighlight` — thin wrapper around `traverseUpstream` that returns the set of highlighted node ids (target + every reachable ancestor), the set of highlighted edge ids, and the raw traversal for downstream path/details panels; short-circuits with empty sets for missing ids, empty inputs, and unsupported node kinds (host/vhost/shovel/federation/external); every empty return is a *freshly-allocated* Set so a caller that mutates one result can never corrupt a later one; edgeIds seed with the shortest-path edges from `traversal.paths` AND then expand to include every routing edge (`binds`/`routes`/`alternate-exchange`/`shovels`/`federates`, plus `dead-letter` when `followDeadLetter=true`) whose endpoints are both highlighted — so diamond bindings, multi-key parallel bindings, and shovel/federation fan-in stay connected and no highlighted ancestor is left with all its incident edges dimmed; wired through src/core/graph/index.ts barrel; extends src/ui/components/topologyGraphElements.ts with an `UpstreamHighlightInput` option on `toReactFlowElements` — highlighted nodes keep full opacity and pick up a yellow outer ring (target gets a stronger 3px ring + bold label), off-path nodes drop to 0.2 opacity; highlighted edges get thicker stroke, off-path edges + labels dim to 0.15 opacity; `FlowNodeData.highlightState` (`target|on-path|off-path`) is exposed for downstream tests/tooling; TopologyGraphCanvas now tracks a selected node id, computes the highlight via `computeUpstreamHighlight`, wires `onNodeClick` (toggle same node = clear) + `onPaneClick` (clear) + a "Clear selection" button, and renders a summary bar with ancestor/edge counts and a truncation flag; 8 pure-function tests in test/core/graph/upstreamHighlight.test.ts cover missing/empty/host targets, queue upstream (ignores parallel branches), exchange upstream, traversal passthrough, a **diamond regression** proving every routing edge between highlighted ancestors ends up in `edgeIds` and every ancestor is touched by at least one highlighted edge, an off-path exclusion assertion, and a fresh-Set-per-call regression proving `EMPTY_HIGHLIGHT` never shares mutable state between callers; 2 new pure-function tests in test/ui/components/topologyGraphElements.test.ts verify target/on-path/off-path styling + edge width boost + label dimming, and confirm an empty highlight is treated as no selection)
- [ ] Highlight complete bidirectional message-flow chains for selected shovels, queues, and exchanges. Extend the current upstream-only selection highlight so selecting a shovel highlights every reachable incoming and outgoing configured-flow relation across the full chain—for example `exchange → exchange → shovel → exchange → queue`—and apply the same behavior to selected queues and exchanges for a clearer end-to-end topology view. Traverse only message-flow/routing edges (`binds`, `routes`, `alternate-exchange`, `shovels`, and `federates`, with dead-letter edges following the existing opt-in behavior), never structural `contains` edges; preserve edge direction while walking both upstream and downstream; include all reachable branches and every connecting relation rather than only one shortest path; remain cycle-safe and honor the existing max-depth limit with a clear truncation indicator. The highlight must compose with broad graph filters and visibility/isolation state so filtered or hidden entities are not resurrected, unsupported node kinds remain safe no-ops, and stale worker results cannot overwrite a newer selection. Update the selection summary/path explanation to distinguish incoming and outgoing reach, and add pure traversal/highlight, worker/hook, rendering, and canvas-pipeline regressions covering the full shovel-chain example, a middle exchange, a queue with both upstream and downstream relations, branching paths, cycles, depth truncation, filters/visibility, and exclusion of unrelated edges.
- [x] Add entity details panel. (adds pure src/ui/components/entityDetails.ts exporting `describeEntity`, `EntityDetailView`, `EntityDetailSection`, `EntityDetailRow` — kind-dispatched formatter turning a `GraphNode` into a structured `{ title, kindLabel, sections: [{ heading, rows: [{ key, value }] }] }` view: hosts render name/cluster/environment/source-file count, vhosts render name+host id, exchanges render type/durable/auto-delete/internal/alternate-exchange/host+vhost ids with a separate Arguments section for `x-*` args, queues add dead-letter exchange + routing key + typed argument stringification (numbers/booleans passthrough, objects JSON-encoded), shovels split into Shovel/Source/Destination + Arguments sections, federation adds federated exchange/queue + Upstream/Downstream endpoints, external nodes render the redacted `EndpointRef` (host/vhost/exchange/queue/uri/unresolved); all string values pass through unchanged because `buildGraph` deep-sanitizes credentials from `GraphNode.data` at the boundary, so this layer never re-implements AMQP-URI redaction; adds React panel src/ui/components/EntityDetailsPanel.tsx rendering the view as a `<aside>` with kind badge (`entity-details-kind`), title (`entity-details-title`), and per-section `<dl>` (`entity-details-section-<kebab-heading>`), plus an empty-state hint when no node is selected; wired into TopologyGraphCanvas below the graph, driven by the same selection state that powers the upstream highlight; 6 pure-function tests in test/ui/components/entityDetails.test.ts cover host/exchange/queue/shovel/federation/external formatting (including redacted-URI passthrough and default-boolean rendering), and 4 jsdom component tests in test/ui/components/EntityDetailsPanel.test.tsx cover empty-state, structured-row rendering for an exchange, multi-section rendering for a shovel, and Arguments-section suppression for entities without arguments)
- [x] Add path explanation panel. (adds src/ui/components/PathExplanationPanel.tsx — thin `<aside>` renderer that wraps `explainUpstreamPath` for every path in a traversal result: shows one section per source→target route with a verb-first English sentence per hop (routing-key aware for `binds`/`routes`, dead-letter/alternate-exchange/shovel/federation phrasing preserved from the existing formatter), a header with total path count + `truncated at max depth` suffix when the traversal was cut short, per-path testids (`path-explanation-item-<i>` / `-step-<j>`) for reliable assertions, and two distinct empty states — hinting the user to select a queue/exchange when no traversal is provided, or reporting `No upstream publishers` when the selected node has no ancestors; each rendered path uses a composite React key of `source→target|<edge-id-sequence>#<index>` so two paths sharing endpoints but traversing different intermediate edges (multi-key parallel bindings, diamond branches) never collide during reconciliation, with the trailing index as a final tiebreaker; `maxPaths` (default 12) is normalized via `normalizeMaxPaths` — non-finite (NaN/±Infinity) → default, negative → 0, fractional → floor — so surprising `slice()` semantics from negative or non-integer values are impossible; hidden-path note reports the actual cap that was applied; sentences and IDs pass through as-is because `explainUpstreamPath` already sanitizes control characters in labels/routing keys via `sanitizeInline`, so credentials or newlines in raw exports cannot break the layout; wired into TopologyGraphCanvas below the entity details panel, driven by the same highlight state so selecting a queue/exchange populates both details + upstream explanation with no extra clicks; 6 jsdom component tests in test/ui/components/PathExplanationPanel.test.tsx cover the no-traversal empty state, the zero-paths empty state, a two-step path with routing-key rendering + path count, a maxPaths cap that reports truncated count + hidden-path note, a **duplicate-source/target regression** proving two paths sharing endpoints but different intermediate edges each render as distinct list items with no React duplicate-key console warnings, and a maxPaths-normalization regression covering negative → 0, fractional → floor, and NaN → default)
- [x] Add filters for host, vhost, entity type, edge type, routing key, and depth. (adds pure src/core/graph/filterGraph.ts exporting `applyGraphFilters` + `GraphFilters` — two-pass reducer that first prunes nodes by host/vhost/kind whitelists (empty sets ⇒ no filter; external nodes bypass host/vhost filters because they carry no in-project host id; host nodes under a vhost filter are retained iff they parent at least one whitelisted vhost (derived from the raw node set via `collectParentHostIdsOfWhitelistedVhosts` so a dangling/unknown id in the whitelist cannot resurrect an unrelated host, and hosts with all their vhosts filtered out are pruned rather than left as orphan headers)), then prunes edges by kind + dangling-endpoint check + case-insensitive routing-key substring (only edges that carry a `routingKey` participate in the routing-key check — kinds without one pass through unaffected so removing them requires an explicit edge-kind filter; whitespace-only queries no-op after trim); returns a fresh `BuildGraphResult` so the output can be handed straight to `toReactFlowElements` or `computeUpstreamHighlight`; extends src/core/graph/index.ts barrel; adds src/ui/components/TopologyFiltersPanel.tsx — controlled filter bar with per-host/per-vhost checkboxes derived from the graph, static entity-kind and edge-kind checkbox rows, a case-insensitive routing-key text input, a 0..128 upstream-depth slider (default 32) with `clampDepth` that maps NaN/Infinity → default, negative → 0, out-of-range → cap, and fractional → floor, and a reset button that emits a fresh empty `FilterState`; every user interaction calls `onChange` with a freshly-allocated Set so the input state is never mutated; `toGraphFilters` trims the routing-key query so a whitespace-only field never excludes every edge; TopologyGraphCanvas now applies the filters to `rawGraph → graph` via `applyGraphFilters(toGraphFilters(state))`, passes `filters.maxDepth` into `computeUpstreamHighlight`, keeps the selected node lookup on the un-filtered graph so a filter that hides the selection still shows details, and renders a "N nodes · M edges (of X · Y)" ratio in the stats line; 11 core tests in test/core/graph/filterGraph.test.ts cover unset/empty-set no-op, host whitelist + dangling-edge pruning, external node retention under host filter, entity-kind whitelist, edge-kind whitelist, routing-key substring + non-routing pass-through, whitespace-only routing-key no-op, host+edge-kind composition, vhost filter pruning unrelated hosts (regression pin for the previous "always keep every host" bug), and unknown-vhost-id whitelist pruning every host so no orphan headers survive; 7 jsdom component tests in test/ui/components/TopologyFiltersPanel.test.tsx cover control rendering, immutable host toggle, raw routing-key propagation, depth clamping/flooring, reset-to-empty, `toGraphFilters` whitespace handling, and empty-graph fallback rendering; 8 pipeline integration tests in test/ui/components/topologyGraphCanvasPipeline.test.ts wire `applyGraphFilters → toReactFlowElements` (host filter drops rendered nodes/edges, vhost filter prunes childless hosts, edge-kind filter drops non-routing edges from the flow) and `applyGraphFilters → computeUpstreamHighlight({maxDepth})` (full-depth highlights every ancestor in a 3-hop chain, depth=1 truncates after one hop, depth=0 highlights only the target, and filter+highlight compose so filtering out routing edges collapses the highlight down to the target))

## Tests and quality

- [x] Add tests for parser fixtures. (adds test/fixtures/minimalDefinitionsFixture.test.ts — dedicated end-to-end coverage that drives `minimal-definitions.json` through `parseDefinitionsExport → parseRuntimeParameters → buildGraph → upstreamForQueue` and asserts every shape documented in test/fixtures/README.md: no error-severity diagnostics from either parser, both vhosts (`/`, `orders`) enumerated, exchange type parsing (direct/topic/fanout) per name, `alternate-exchange` argument on `orders.in`, both queue- and exchange-destination bindings including the exchange→exchange `orders.in → orders.audit` route, dead-letter policy captured verbatim while per-queue DLX metadata stays unset (policy application is a RabbitMQ runtime concern our parser deliberately does not synthesize), runtime parameter component set exactly `{shovel, federation-upstream}`, shovel materialized with sanitized endpoints referencing `remote-host-a.example.internal`, federation-upstream materialized with a redacted uri referencing `remote-host-b.example.internal`, buildGraph emits nodes for every kind exercised by the fixture (host/vhost/exchange/queue/shovel/federation) and edges for every kind the fixture documents (`contains`/`binds`/`alternate-exchange`/`shovels`/`federates`), upstream traversal from `orders.incoming` reaches `orders.in` via a `binds` step, shovel node destination references `orders.in`, and the full node+edge JSON never contains a raw `user:pass@` AMQP userinfo pattern)
- [x] Add tests for topic routing matcher. (already covered end-to-end by test/core/graph/topicMatcher.test.ts — 24 pure tests spanning exact/no-wildcards, `*` single-word, `#` zero-or-more-words including adversarial repeated-`#` bounded-time pins, combined `*`+`#`, leading/trailing/consecutive-dot empty-word semantics, and `isTopicPattern` classification; adds test/core/graph/topicMatcherFixture.test.ts (8 additional fixture-driven cases) that feeds every distinct binding routing key from `minimal-definitions.json` through `parseDefinitionsExport` and asserts real-broker semantics on the resulting patterns: `orders.#` matches every `orders.*` key including the exact `orders`, `orders.*` matches exactly one word after `orders`, empty-string routing keys only match empty patterns, the literal `jobs` pattern matches only itself, `isTopicPattern` correctly classifies every fixture pattern, every literal fixture pattern matches itself and rejects any single-word suffix, and every wildcard fixture pattern routes the plausible `orders.new` message key — so a regression in word tokenisation or wildcard handling fails against real project data, not just synthetic input)
- [x] Add tests for queue upstream traversal. (already covered by test/core/graph/traversal.test.ts — 15 pure-graph tests spanning exchange→exchange→queue chains, non-queue target rejection, unknown-id safety, maxDepth clamping (default/negative/fractional/±Infinity), shovel + external endpoint, diamond graphs, alternate-exchange, federation, dead-letter opt-in on/off, invalid maxDepth normalization, and cycle guarding; adds test/core/graph/upstreamForQueueFixture.test.ts (7 fixture-driven cases) that drives the real `parseDefinitionsExport → parseRuntimeParameters → buildGraph` pipeline over `minimal-definitions.json` and asserts: `work.jobs` reaches `work.direct` as its sole ancestor with one representative single-step `binds` path carrying routing key `jobs`, `truncated=false` + no cycle-guard witnesses; `orders.audit` queue reaches BOTH the same-named fanout exchange and (via exchange-to-exchange binding) `orders.in`; `work.dead` reaches `work.dlx` with the empty-routing-key fanout step preserved; non-queue targets (exchange id) and unknown ids return empty ancestor sets without throwing; and `maxDepth=1` on the two-hop `orders.audit` chain truncates before reaching `orders.in` with `truncated=true`)
- [x] Add tests for exchange upstream traversal. (already covered by test/core/graph/upstreamForExchange.test.ts — 6 pure-graph cases spanning an exchange→exchange→exchange chain (full ancestry + representative source path), target-kind guarding (queue/unknown/host all yield empty), and shovel+federation into an exchange target; adds test/core/graph/upstreamForExchangeFixture.test.ts (7 fixture-driven cases) that drives the real `parseDefinitionsExport → parseRuntimeParameters → buildGraph` pipeline over `minimal-definitions.json` and asserts: `orders.audit`-exchange reaches `orders.in` via a single `binds` step with routing key `orders.*`, no truncation and no cycle witnesses; `orders.unrouted` reaches `orders.in` via an `alternate-exchange` step; queue-as-target (`work.jobs`) and unknown-id targets return empty ancestor sets without throwing; the root publisher exchange `work.direct` has an empty upstream ancestry (fixture only binds queues from it, never anything to it); and pins the actual boundary behavior for `orders.in` — its in-project reverse traversal terminates as a self-source because the fixture shovel's `dest-uri` uses `localhost` which resolves to an external node rather than back into `orders.in`)
- [x] Add tests for cross-host shovel traversal. (already covered by test/core/graph/crossHost.test.ts — 5 cases: shovel-crossing walk, federation-crossing summary, single-host baseline, external-hostname-hint fallback, and cross-host summary identification; adds test/core/graph/crossHostShovelScenarios.test.ts (5 scenario cases) that broaden coverage to shapes not exercised by the baseline: a three-host A→B→C shovel chain reaching every intermediate exchange + both shovels while the summary lists both ancestor hosts, a queue-source shovel that drains a queue on rabbit-a into an exchange on rabbit-b and still surfaces as cross-host, a same-host cross-vhost shovel that is NOT cross-host per `ancestorHostIds` but still marked `crossedShovelOrFederation=true`, two shovels fanning into one downstream exchange producing distinct source paths + a two-host `ancestorHostIds` set, and a shovel whose source references an unloaded remote host that resolves to an external ancestor node with the hostname surfaced through `summary.externalHostHints`)
- [x] Add tests for federation traversal. (adds test/core/graph/federationTraversal.test.ts — 9 focused cases exercising the `federates` edge kind end-to-end: queue-target walk reaches the upstream exchange through mirror→federates(out)→federation→federates(in)→publish; every `federates` step carries the federation link's `name` as its label; routing-key metadata on the mirror binding survives the composed path; exchange-target upstream traversal reaches the federation ancestor; dropping the upstream host produces an `external:` ancestor node whose id embeds the source hostname; two federations feeding the same downstream produce two distinct source paths; cross-vhost federation on the same host walks between two vhosts; `maxDepth=1` truncates before reaching the upstream exchange with `truncated=true`; and a pathological loop where the downstream mirror binds back to the upstream publisher forms a cycle that `findCycles` detects across the SCC spanning both exchanges + the federation node while the bounded traversal still terminates)
- [x] Add tests for cycle detection. (already covered by test/core/graph/cycles.test.ts — 10 pure cases: DAG baseline, 2-node cycle, 3-node cycle, non-Hamiltonian SCC + witness validity, multi-cycle SCC via shared bridge, self-loop, cross-host cycle via federation, edge-kind restriction (dead-letter excluded), K_30 dense-SCC witness under 100ms, and the maxDepth regression pin; adds test/core/graph/cyclesScenarios.test.ts (6 additional cases): the sanitized `minimal-definitions.json` fixture is acyclic; an N=4 ring reports one SCC covering every node with a witness whose edges all resolve to real graph edges; an N=8 ring stays O(V+E) and completes in under 50ms with a single 8-node SCC; two disjoint 2-node cycles (A↔B and C↔D) produce two independent Cycle reports; a `binds`-only A↔B cycle disappears when the edgeKinds option excludes `binds`; and fully dangling edges (both endpoints unknown, or half-dangling) are filtered out so cycle detection over a stray-edge-only input returns [] without throwing)
- [x] Add build/typecheck gate in CI-ready script. (scripts/verify.sh already runs the full gate — sanitized `data/raw` preflight, AMQP credential scan across every text file, `npm test`, `npm run typecheck`, `npm run build` — with `set -euo pipefail` so any failure exits non-zero; adds .github/workflows/ci.yml wiring the gate to GitHub Actions on push to main/master and every pull request: Ubuntu runner, `actions/setup-node@v4` with Node 20 + npm cache, `npm ci` for reproducible installs, then `bash scripts/verify.sh`; `concurrency` group cancels in-flight runs on force-push, 15-minute job timeout guards against hung builds; adds an `npm run verify` alias in package.json so contributors can invoke the identical gate locally in one command)

## Later options

- [x] Move parsing/query work into a Web Worker for large topologies. (adds src/core/import/importArchiveWorkerMessage.ts exporting `WorkerRequest` / `WorkerResponse` discriminated unions covering four kinds — `import` (parse an archive), `build-graph` (run the graph builder), `upstream-for-queue`, and `upstream-for-exchange` — plus back-compat `ImportArchiveWorker{Request,Response}` aliases; the pure async `handleImportArchiveMessage(request: unknown)` dispatcher does EVERY payload read (including the `id`, `kind`, and per-kind `input` fetches) inside a single try/catch so `null`, `undefined`, primitives, or requests missing required fields never escape as an unhandled worker rejection — errors return a structured `status:"error"` envelope with `id: -1` when the payload lacked one; adds src/core/import/importArchiveWorker.ts — the Vite worker entry (guarded by `/// <reference lib="webworker" />`) that wires `addEventListener("message")` + `postMessage` to the dispatcher; Vite recognises the `new Worker(new URL('./importArchiveWorker.ts', import.meta.url), { type: 'module' })` pattern used by `getSharedTopologyWorkerClient` and code-splits it into its own bundle (dist/assets/importArchiveWorker-*.js — verified by `npm run build`); adds src/core/import/importArchiveWorkerClient.ts exporting: (1) `createImportArchiveWorkerClient({createWorker})` — Promise-based wrapper with monotonic per-request ids for concurrent operations, id-dispatched incoming messages, `postMessage` calls wrapped in try/catch so a synchronous clone/terminate throw removes the pending entry and rejects the caller instead of hanging forever, worker `error` + `messageerror` event listeners that reject EVERY pending request with a descriptive message AND drive the client into a terminal `failure` state so every subsequent `submitRaw` request rejects immediately with the failure reason instead of posting to a dead worker that would never respond (also detaches listeners, tears down the underlying worker, and fires the optional `onFailure(error)` hook exactly once — `getSharedTopologyWorkerClient` wires this to invalidate the cached singleton so the next call spins up a fresh worker or falls back to the main-thread client), and a `terminate()` that removes every listener + calls `worker.terminate?.()` inside a `try/catch` whose `finally` block always rejects still-pending requests before rethrowing any teardown error, so a throwing `worker.terminate()` (some browser polyfills throw on already-terminated workers, and shims may too) can never leave awaiters hanging; (2) `importOnMainThread(input)` synchronous fallback; (3) `createMainThreadClient()` mirroring the worker client's four-method shape for environments without Worker — its internal `guard()` wraps `work()` in `try/catch` before `Promise.resolve` so a synchronous throw from `buildGraph` or the traversal helpers is translated into a rejected Promise rather than escaping the async boundary; (4) `getSharedTopologyWorkerClient()` / `resetSharedTopologyWorkerClient()` — lazy singleton that tries the real Vite worker constructor first and transparently falls back to the main-thread client when `Worker` is undefined or construction throws; wires production callers to the shared client: ImportPanel (src/ui/components/ImportPanel.tsx) routes archive parsing through `getSharedTopologyWorkerClient().importArchive(...)`, and a new `useTopologyGraph` hook (src/ui/hooks/useTopologyGraph.ts) routes `buildGraph` + `upstreamForQueue` + `upstreamForExchange` through the shared client — TopologyGraphCanvas (src/ui/components/TopologyGraphCanvas.tsx) is refactored to consume this hook so its previously-synchronous graph build + upstream traversal now run off-thread, with per-effect `cancelled` flags so a fast follow-up selection or filter change never resolves stale state, the build effect clears `rawGraph` + `highlight` + `highlightLoading` synchronously the moment `result` changes so no stale topology (or stale highlight) is exposed to the UI while the new worker build is in flight, AND the traversal effect clears the previous `highlight` synchronously the moment a new traversal begins (selection changed, filter maxDepth changed, or filtered graph changed) so a stale node/edge highlight from the prior selection never remains rendered while the deferred worker traversal is in flight, and a `workerClient?` prop injection point for tests; extracts a pure `highlightFromTraversal(input, targetNodeId, traversal, followDeadLetter)` helper from `computeUpstreamHighlight` so the same highlight sets can be built locally from the worker's `UpstreamTraversalResult` payload; stats line surfaces `building graph…` / `computing upstream…` progress hints; extends src/core/import/index.ts barrel with the message + client modules only (worker entry is import-URL-only); 23 unit tests in test/core/import/importArchiveWorker.test.ts + 8 hook tests in test/ui/hooks/useTopologyGraph.test.tsx (jsdom + `renderHook` from `@testing-library/react`) proving that production graph and traversal work is actually routed through the worker client — `buildGraph` is called on mount and its result populates state, selecting a queue triggers `upstreamForQueue` (not the exchange variant) and vice versa, no selection means neither traversal method is called, `filters.maxDepth` is threaded through as an `UpstreamTraversalOptions.maxDepth`, a rejected `buildGraph` falls back to an empty graph without crashing the UI, and a deferred-build regression pin that mocks two consecutive buildGraph calls (each held open until the test releases them) — asserts that swapping the `result` prop while the second build is still pending immediately clears `rawGraph` + `highlight`, sets `buildLoading=true`, and never exposes the previously-selected node from the first topology; and a deferred-traversal regression pin that mocks two consecutive `upstreamForQueue` calls (each held open until the test releases them) — resolves the first with a q1-centric highlight, then rapidly switches selection to q2 while the second traversal is still pending, asserts that `highlight.nodeIds.size === 0`, `highlight.edgeIds.size === 0`, and neither `queue:h:q1` nor `b:x->q1` is still highlighted at the swap moment (the exact stale-highlight exposure the reviewer flagged); then releases the second traversal and confirms the q2-centric highlight takes over cover the pure dispatcher (import round-trip, build-graph dispatch, upstream-for-queue dispatch, `null`/`undefined`/primitive payload rejection with structured envelope, unknown-kind rejection preserving id, per-kind `missing input` error), the client contract via an in-process shim worker (single request round-trip, three concurrent requests get distinct monotonic ids `[1,2,3]`, `status:"error"` responses reject, `terminate()` rejects pending + refuses further calls + terminates the underlying worker, a **real stale-response regression** that fires both an unknown-id and a duplicate-id response after the pending entry is consumed and asserts neither throws nor resolves a subsequent request incorrectly, worker `error` event rejects every pending request, worker `messageerror` event rejects with a descriptive message, `postMessage` throwing synchronously removes the pending entry and rejects the caller, and an end-to-end upstream-for-queue path through the worker), the fallback layer (`importOnMainThread` shape parity, `createMainThreadClient` four-method parity + terminate guard, `getSharedTopologyWorkerClient` transparent fallback when `Worker` is undefined), and two Promise-safety regression pins: `createMainThreadClient` returns a *rejected* Promise (never throws synchronously) when `buildGraph`/`upstreamForQueue` are invoked with invalid input that would otherwise throw synchronously, a `terminate()` regression where a shim whose `worker.terminate()` throws still rejects every pending request via the `finally` block before rethrowing the teardown error to the caller, and three terminal-failure regressions that prove (a) a `error` event puts the client into a failed state where every subsequent request rejects immediately without posting to the dead worker, `onFailure` fires exactly once even if `error` is re-fired later, (b) the same guarantee holds after a `messageerror` event, and (c) `getSharedTopologyWorkerClient()` returns a fresh client after `resetSharedTopologyWorkerClient()` (the same invalidation path the `onFailure` hook wires up in production) and the replacement client still services a real import end-to-end)
- [x] Add graph neighborhood pruning for very large graphs. (adds pure src/core/graph/pruneNeighborhood.ts exporting `pruneNeighborhood`, `PruneNeighborhoodOptions`, `PruneNeighborhoodResult`, `NeighborhoodDirection` — restricts a full `BuildGraphResult` to the N-hop neighborhood around a focus node via BFS over the routing edge set (`binds`/`routes`/`alternate-exchange`/`shovels`/`federates`, plus `dead-letter` when `followDeadLetter=true`); `direction` picks `upstream`/`downstream`/`both` (default `both`); `maxDepth` normalizes NaN/±Infinity → default 3, negative → 0, fractional → floor; a second pass optionally walks structural `contains` ancestry so each surviving entity keeps its parent vhost/host visible (defaults on; disable with `keepContainsAncestry:false`) with an 8-hop cap on the structural walk to guard against pathological `contains` loops; every output edge has both endpoints in the kept node set (no dangling handles), diagnostics pass through by reference, and `truncated=true` is only set when a node at the depth cap has an incident routing edge whose *other endpoint is not already kept* (checking edge presence alone would incorrectly flip the flag for a diamond/cycle whose neighbors are already in the neighborhood — e.g. focus=A with A→B at maxDepth=1 must retain the full graph and stay non-truncated); extends src/core/graph/index.ts barrel; 18 unit tests in test/core/graph/pruneNeighborhood.test.ts cover: focusMissing (undefined + unknown id) short-circuits, upstream-only walk from a queue reaches only ancestors, downstream-only walk from x0 reaches only descendants, both-direction walk from a middle node reaches both sides within depth, `maxDepth=0` keeps only the focus + structural ancestry with `truncated=true`, `truncated=false` when depth exceeds the deepest branch, negative/fractional/NaN maxDepth normalization pins, dead-letter opt-in on/off pin, structural ancestry keeps host+vhost by default while excluding orphan siblings and can be stripped explicitly, no-dangling-edge invariant, diagnostics ref-passthrough, a 2000-node chain performance regression pin completing at depth 10 in well under 100 ms with exactly 21 kept nodes, and three truncation-flag regression pins covering the reviewer-supplied `A→B` two-node case with focus A + direction both + maxDepth=1 (retains {A,B}, `truncated=false`), a 4-node chain whose deepest node sits exactly at `maxDepth` (`truncated=false`), and a diamond `A→{B,C}→D` whose depth-2 ancestors' only incoming edges land on already-kept A (`truncated=false`))
- [x] Evaluate Cytoscape.js if React Flow struggles with large datasets. (adds docs/render-engine-evaluation.md — decision doc, explicitly not a benchmark report; current decision is stay on React Flow; documents the accurate current architecture: worker-side request kinds are exactly `import` / `build-graph` / `upstream-for-queue` / `upstream-for-exchange`, `applyGraphFilters` runs main-thread, `filters.maxDepth` is passed to the worker traversal call as `UpstreamTraversalOptions.maxDepth` (not to `computeUpstreamHighlight`), and the returned `UpstreamTraversalResult` is assembled into an `UpstreamHighlight` by the local `highlightFromTraversal` helper — `computeUpstreamHighlight` remains as a synchronous alternative but is no longer in the async production path, and `pruneNeighborhood` is a pure utility that is NOT wired into the UI yet; tabulates qualitative Cytoscape.js tradeoffs (canvas rendering, built-in layouts vs React interop + existing tests) and marks bundle-size + migration-effort figures as unmeasured planning estimates; pins four numeric switching thresholds — interaction latency > 200 ms P95, initial paint after the worker resolves > 1 s, resident memory > 500 MB, dropped-frame ratio > 30 % during a 5 s pan/zoom — and states explicitly that a switch is only proposed when **at least one** threshold is crossed and reads "below **all** of these" when they are not; TL;DR references the numeric thresholds instead of restating them so the "build time" vs "initial paint" wording mismatch is gone; adds a "How to measure" section with a Chrome DevTools recipe that logs results in `docs/render-engine-benchmarks/<YYYY-MM-DD>.md` for reproducible follow-up; keeps the seven-step migration plan gated on measured evidence; records "not switching now" as the current decision)
- [x] Add Tauri/Electron packaging if browser file APIs are limiting. (adds docs/packaging-evaluation.md — decision doc, explicitly not a benchmark or spike report; current decision is stay on the browser static build because every import shape the app supports today (single-file `<input type="file">` + `File.arrayBuffer()` in ImportPanel, JSZip / node-unrar-js archive decompression fully in-JS, off-thread parsing via the shared Web Worker client) works cross-browser without any filesystem-watcher or native-storage capability; documents the accurate current app→host surface (read one picked file, run parsing off-thread, no persistence, no writes outside `data/raw/`) and confirms nothing has hit a browser-API wall; tabulates qualitative Tauri vs Electron vs browser capabilities marking bundle-size / RAM figures as unmeasured planning estimates; enumerates candidate wall-hitting cases (folder-drop batch import across Firefox/Safari, filesystem watcher for over-time comparison, cross-session persistence) and rates each as not-blocked-yet; pins three switching criteria (a backlog user story needing a filesystem watcher or "point at a folder" batch import not supported cross-browser, roadmap addition requiring persistence that IndexedDB can't cover, or air-gapped distribution to a browserless environment) with the "at least one crossed" / "below all" logic; recommends Tauri over Electron on a qualitative documented-runtime-architecture comparison (OS webview reuse vs bundled Chromium, Rust vs Node backend as neutrally-framed ecosystem tradeoffs rather than security assertions) when the switch is made; sketches a six-step migration plan gated on measured evidence (spike `tauri init`, introduce a `Host` filesystem abstraction consumed by ImportPanel, keep the Web Worker unchanged, add a Tauri-signed release job to .github/workflows/ci.yml); adds a "How to measure before proposing a switch" section requiring a specific user story + demonstration that the browser path is insufficient + installer-size / cold-start-memory log under `docs/packaging-benchmarks/<YYYY-MM-DD>.md`; records "not packaging as Tauri or Electron now" as the current decision)
